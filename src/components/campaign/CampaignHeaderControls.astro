---
interface Props {
  variant: 'home' | 'join';
}

const { variant } = Astro.props;
---
<div
  data-header-preview-controls-loader
  data-controls-src={`/ui/header-preview-controls/${variant}`}
></div>
