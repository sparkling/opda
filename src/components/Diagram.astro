---
/**
 * Mermaid diagram figure — wraps the source and its visible caption so the
 * shared interactive renderer can preserve one accessible figure boundary.
 *
 *   <Diagram size="medium" caption="Two pathways, two authorities.">
 *     flowchart LR
 *       A --> B
 *   </Diagram>
 *
 * `caption` is rendered with `set:html` so inline tags (<code>, <a>, <strong>)
 * inside captions are preserved. `size` defaults to the unconstrained
 * default (max-width 100%, max-height 750px).
 */
interface Props {
  size?: 'small' | 'medium' | 'large';
  caption?: string;
}
const { size, caption } = Astro.props;
const cls = ['diagram', size && `diagram--${size}`].filter(Boolean).join(' ');
---
<figure class={cls}>
  <div class="mermaid" data-node-interaction="static">
    <slot />
  </div>
  {caption && <figcaption class="diagram-caption" set:html={caption} />}
</figure>
