<section class="method-flow" aria-labelledby="method-title">
  <div class="method-flow__inner">
    <header class="method-flow__header">
      <p class="method-flow__overline">The method</p>
      <h2 id="method-title">From evidence to shared meaning</h2>
      <p>
        Industry evidence and professional expertise are reviewed by the people who
        understand each part of a property transaction. Agreed meaning is then recorded
        with a visible decision and status.
      </p>
    </header>

    <figure class="method-flow__figure">
      <svg
        class="method-flow__svg method-flow__svg--wide"
        viewBox="0 0 960 230"
        role="img"
        aria-labelledby="method-flow-wide-title"
        aria-describedby="method-flow-wide-desc"
        focusable="false"
      >
        <title id="method-flow-wide-title">How SPDTF is developed</title>
        <desc id="method-flow-wide-desc">
          Four stages in sequence: evidence and expertise, working-group review, a
          governed candidate as the decision point, and shared property information.
        </desc>

        <g class="method-flow__connectors" aria-hidden="true">
          <line x1="222" y1="120" x2="250" y2="120" />
          <polygon points="250,114 258,120 250,126" />
          <line x1="462" y1="120" x2="490" y2="120" />
          <polygon points="490,114 498,120 490,126" />
          <line x1="702" y1="120" x2="730" y2="120" />
          <polygon points="730,114 738,120 730,126" />
        </g>

        <g class="method-flow__stage">
          <rect class="method-flow__box" x="18" y="60" width="204" height="120" />
          <text class="method-flow__number" x="34" y="84">01</text>
          <text class="method-flow__title" x="34" y="111">
            <tspan x="34">Evidence &amp;</tspan>
            <tspan x="34" dy="23">expertise</tspan>
          </text>
          <text class="method-flow__body" x="34" y="157">
            <tspan x="34">Industry input and</tspan>
            <tspan x="34" dy="18">existing records</tspan>
          </text>
        </g>

        <g class="method-flow__stage">
          <rect class="method-flow__box" x="258" y="60" width="204" height="120" />
          <text class="method-flow__number" x="274" y="84">02</text>
          <text class="method-flow__title" x="274" y="111">
            <tspan x="274">Working-group</tspan>
            <tspan x="274" dy="23">review</tspan>
          </text>
          <text class="method-flow__body" x="274" y="157">
            <tspan x="274">Domain experts agree</tspan>
            <tspan x="274" dy="18">meaning</tspan>
          </text>
        </g>

        <g class="method-flow__stage method-flow__stage--decision">
          <rect class="method-flow__box" x="498" y="60" width="204" height="120" />
          <line class="method-flow__rule" x1="498" y1="62" x2="702" y2="62" />
          <text class="method-flow__number" x="514" y="84">03</text>
          <text class="method-flow__title" x="514" y="111">
            <tspan x="514">Governed</tspan>
            <tspan x="514" dy="23">candidate</tspan>
          </text>
          <text class="method-flow__body" x="514" y="157">
            <tspan x="514">Recorded decision and</tspan>
            <tspan x="514" dy="18">status</tspan>
          </text>
        </g>

        <g class="method-flow__stage">
          <rect class="method-flow__box" x="738" y="60" width="204" height="120" />
          <text class="method-flow__number" x="754" y="84">04</text>
          <text class="method-flow__title" x="754" y="111">
            <tspan x="754">Shared property</tspan>
            <tspan x="754" dy="23">information</tspan>
          </text>
          <text class="method-flow__body" x="754" y="157">
            <tspan x="754">One language for</tspan>
            <tspan x="754" dy="18">exchange</tspan>
          </text>
        </g>
      </svg>

      <svg
        class="method-flow__svg method-flow__svg--stacked"
        viewBox="0 0 360 752"
        role="img"
        aria-labelledby="method-flow-stacked-title"
        aria-describedby="method-flow-stacked-desc"
        focusable="false"
      >
        <title id="method-flow-stacked-title">How SPDTF is developed</title>
        <desc id="method-flow-stacked-desc">
          Four stages in sequence: evidence and expertise, working-group review, a
          governed candidate as the decision point, and shared property information.
        </desc>

        <g class="method-flow__connectors" aria-hidden="true">
          <line x1="180" y1="158" x2="180" y2="201" />
          <polygon points="174,201 180,209 186,201" />
          <line x1="180" y1="350" x2="180" y2="393" />
          <polygon points="174,393 180,401 186,393" />
          <line x1="180" y1="542" x2="180" y2="585" />
          <polygon points="174,585 180,593 186,585" />
        </g>

        <g class="method-flow__stage">
          <rect class="method-flow__box" x="18" y="18" width="324" height="140" />
          <text class="method-flow__number method-flow__number--stacked" x="38" y="48">01</text>
          <text class="method-flow__title method-flow__title--stacked" x="38" y="81">
            <tspan x="38">Evidence &amp; expertise</tspan>
          </text>
          <text class="method-flow__body method-flow__body--stacked" x="38" y="113">
            <tspan x="38">Industry input and existing records</tspan>
          </text>
        </g>

        <g class="method-flow__stage">
          <rect class="method-flow__box" x="18" y="210" width="324" height="140" />
          <text class="method-flow__number method-flow__number--stacked" x="38" y="240">02</text>
          <text class="method-flow__title method-flow__title--stacked" x="38" y="273">
            <tspan x="38">Working-group review</tspan>
          </text>
          <text class="method-flow__body method-flow__body--stacked" x="38" y="305">
            <tspan x="38">Domain experts agree meaning</tspan>
          </text>
        </g>

        <g class="method-flow__stage method-flow__stage--decision">
          <rect class="method-flow__box" x="18" y="402" width="324" height="140" />
          <line class="method-flow__rule" x1="18" y1="404" x2="342" y2="404" />
          <text class="method-flow__number method-flow__number--stacked" x="38" y="432">03</text>
          <text class="method-flow__title method-flow__title--stacked" x="38" y="465">
            <tspan x="38">Governed candidate</tspan>
          </text>
          <text class="method-flow__body method-flow__body--stacked" x="38" y="497">
            <tspan x="38">Recorded decision and status</tspan>
          </text>
        </g>

        <g class="method-flow__stage">
          <rect class="method-flow__box" x="18" y="594" width="324" height="140" />
          <text class="method-flow__number method-flow__number--stacked" x="38" y="624">04</text>
          <text class="method-flow__title method-flow__title--stacked" x="38" y="657">
            <tspan x="38">Shared property information</tspan>
          </text>
          <text class="method-flow__body method-flow__body--stacked" x="38" y="689">
            <tspan x="38">One language for exchange</tspan>
          </text>
        </g>
      </svg>

      <figcaption>
        How SPDTF develops: evidence and professional expertise enter working-group
        review, where domain experts agree meaning. A governed candidate records the
        decision and its status; governed candidates build towards shared property
        information. Publication here shows development in the open, not adoption.
      </figcaption>
    </figure>
  </div>
</section>

<style>
  .method-flow {
    color: var(--color-text);
    background: var(--color-surface-alt);
    border-bottom: 1px solid var(--color-border);
  }

  .method-flow__inner {
    width: min(100%, var(--content-max));
    margin: 0 auto;
    padding: var(--space-10) var(--space-7);
  }

  .method-flow__header { margin-bottom: var(--space-7); }
  .method-flow__overline {
    margin: 0 0 var(--space-4);
    color: var(--color-text-muted);
    font: 700 var(--text-xs) / 1.3 var(--font-sans);
    letter-spacing: var(--tracking-meta);
    text-transform: uppercase;
  }
  .method-flow__header h2 { margin: 0; font: var(--h2); }
  .method-flow__header > p:last-child {
    margin: var(--space-4) 0 0;
    color: var(--color-text-muted);
    font: var(--lead);
  }

  .method-flow__figure {
    margin: 0;
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    background: var(--color-surface-card);
  }
  .method-flow__svg { width: 100%; height: auto; }
  .method-flow__svg--stacked { display: none; }
  .method-flow__box {
    fill: var(--color-surface-card);
    stroke: var(--color-border-strong);
    stroke-width: 1.5;
    vector-effect: non-scaling-stroke;
  }
  .method-flow__stage--decision .method-flow__box {
    stroke: var(--color-text-strong);
    stroke-width: 2;
  }
  .method-flow__rule {
    stroke: var(--brand-yellow);
    stroke-width: 4;
    vector-effect: non-scaling-stroke;
  }
  .method-flow__connectors line {
    stroke: var(--color-text-muted);
    stroke-width: 1.5;
    vector-effect: non-scaling-stroke;
  }
  .method-flow__connectors polygon { fill: var(--color-text-muted); }
  .method-flow__number {
    fill: var(--color-text-muted);
    font: 700 11px / 1 var(--font-mono);
    letter-spacing: 0.08em;
  }
  .method-flow__stage--decision .method-flow__number { fill: var(--color-text-strong); }
  .method-flow__title {
    fill: var(--color-text-strong);
    font: 700 17px / 1.2 var(--font-sans);
  }
  .method-flow__body {
    fill: var(--color-text-muted);
    font: 400 12px / 1.4 var(--font-sans);
  }
  .method-flow__number--stacked { font-size: 15px; }
  .method-flow__title--stacked { font-size: 22px; }
  .method-flow__body--stacked { font-size: 17px; }
  .method-flow__figure figcaption {
    margin-top: var(--space-5);
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
    color: var(--color-text-muted);
    font-size: var(--text-sm);
  }

  @media (max-width: 64rem) {
    .method-flow__svg--wide { display: none; }
    .method-flow__svg--stacked {
      display: block;
      width: min(100%, 30rem);
      margin-inline: auto;
    }
  }

  @media (max-width: 40rem) {
    .method-flow__inner { padding: var(--space-8) var(--space-5); }
    .method-flow__figure { padding: var(--space-4); }
  }

  @media (forced-colors: active) {
    .method-flow__figure,
    .method-flow__box { border-color: CanvasText; }
    .method-flow__box { fill: Canvas; stroke: CanvasText; }
    .method-flow__stage--decision .method-flow__box,
    .method-flow__rule { stroke: Highlight; }
    .method-flow__connectors line { stroke: CanvasText; }
    .method-flow__connectors polygon { fill: CanvasText; }
    .method-flow__number,
    .method-flow__title,
    .method-flow__body { fill: CanvasText; }
  }

  @media print {
    .method-flow { color: black; background: white; }
    .method-flow__overline,
    .method-flow__header h2,
    .method-flow__header > p:last-child,
    .method-flow__figure figcaption { color: black; }
    .method-flow__figure {
      break-inside: avoid;
      border-color: black;
      background: white;
    }
    .method-flow__box,
    .method-flow__stage--decision .method-flow__box { fill: white; stroke: black; }
    .method-flow__number,
    .method-flow__stage--decision .method-flow__number,
    .method-flow__title,
    .method-flow__body { fill: black; }
    .method-flow__rule,
    .method-flow__connectors line { stroke: black; }
    .method-flow__connectors polygon { fill: black; }
  }
</style>
