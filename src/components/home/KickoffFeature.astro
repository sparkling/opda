<section class="kickoff-feature" aria-labelledby="kickoff-title">
  <div class="kickoff-feature__inner">
    <div class="kickoff-feature__copy">
      <header class="section-heading">
        <p class="section-heading__eyebrow">Featured presentation</p>
        <h2 id="kickoff-title">Working group kick-off</h2>
      </header>
      <p class="kickoff-feature__description">
        A non-technical, interactive introduction for people joining any OPDA domain
        working group: why shared property information matters, how groups agree meaning,
        and how to take part in review. It runs full-screen in its own visual style for a
        first session; no modelling background is needed.
      </p>
      <a class="btn" href="/presentation/working-group-kickoff">Open the presentation</a>
    </div>

    <div class="kickoff-feature__poster" aria-hidden="true">
      <div class="kickoff-feature__poster-frame">
        <span>OPDA · Domain working groups</span>
        <strong>Working Group<br />Kick-off</strong>
        <small>Non-technical · Interactive · Full-screen</small>
      </div>
    </div>
  </div>
</section>

<style>
  .kickoff-feature {
    --kickoff-surface: var(--color-surface);
    --kickoff-surface-accent: var(--color-surface);
    --kickoff-ink: var(--color-text-strong);
    --kickoff-muted: var(--color-text-muted);
    --kickoff-poster: var(--color-surface-alt);
    --kickoff-grid: rgb(44 39 59 / 8%);
    --kickoff-frame: var(--color-border-strong);
    --kickoff-ornament: rgb(254 201 43 / 50%);
    position: relative;
    overflow: hidden;
    color: var(--kickoff-ink);
    background: linear-gradient(135deg, var(--kickoff-surface), var(--kickoff-surface-accent));
    border-bottom: 4px solid var(--brand-yellow);
  }
  .kickoff-feature::after {
    content: '';
    position: absolute;
    right: -8rem;
    bottom: -12rem;
    width: 30rem;
    aspect-ratio: 1;
    border: 1px solid var(--kickoff-ornament);
    transform: rotate(45deg);
    pointer-events: none;
  }
  .kickoff-feature__inner {
    position: relative;
    z-index: 1;
    width: min(100%, var(--content-max));
    margin: 0 auto;
    padding: var(--space-10) var(--space-7);
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(0, 0.92fr);
    align-items: center;
    gap: var(--space-10);
  }
  .kickoff-feature__copy { min-width: 0; }
  .kickoff-feature .section-heading { margin-bottom: 0; }
  .kickoff-feature .section-heading :is(h2, h3) { color: var(--kickoff-ink); }
  .kickoff-feature__description {
    margin: var(--space-6) 0 0;
    color: var(--kickoff-muted);
    font: var(--lead);
  }
  .kickoff-feature .btn { margin-top: var(--space-7); }
  .kickoff-feature__poster {
    position: relative;
    order: -1;
    aspect-ratio: 16 / 9;
    padding: var(--space-5);
    border: 1px solid var(--kickoff-frame);
    background:
      linear-gradient(90deg, transparent 49.8%, var(--kickoff-grid) 50%, transparent 50.2%),
      linear-gradient(0deg, transparent 49.8%, var(--kickoff-grid) 50%, transparent 50.2%),
      var(--kickoff-poster);
  }
  .kickoff-feature__poster::before {
    content: '';
    position: absolute;
    inset: var(--space-5) calc(-1 * var(--space-5)) calc(-1 * var(--space-5)) var(--space-5);
    border: 1px solid var(--brand-yellow);
    pointer-events: none;
  }
  .kickoff-feature__poster-frame {
    position: relative;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: clamp(var(--space-5), 3vw, var(--space-8));
    border-top: 4px solid var(--brand-yellow);
  }
  .kickoff-feature__poster span,
  .kickoff-feature__poster small {
    color: var(--kickoff-muted);
    font: 700 clamp(0.6875rem, 1vw, var(--text-xs)) / 1.3 var(--font-mono);
    letter-spacing: var(--tracking-meta);
    text-transform: uppercase;
  }
  .kickoff-feature__poster strong {
    color: var(--kickoff-ink);
    font: 600 clamp(1.75rem, 4.4vw, 4.8rem) / 0.98 var(--font-display);
    letter-spacing: var(--tracking-display);
  }

  :global(:root[data-theme='dark']) .kickoff-feature {
    --kickoff-surface: var(--color-surface);
    --kickoff-surface-accent: var(--color-surface-alt);
    --kickoff-ink: var(--color-text-strong);
    --kickoff-muted: var(--color-text-muted);
    --kickoff-poster: var(--color-surface-card);
    --kickoff-grid: rgb(255 255 255 / 8%);
    --kickoff-frame: var(--color-border);
    --kickoff-ornament: rgb(254 201 43 / 34%);
  }

  :global(:root[data-theme='dark']) .kickoff-feature .section-heading__eyebrow { color: var(--brand-yellow); }

  @media (max-width: 64rem) {
    .kickoff-feature__inner { grid-template-columns: minmax(0, 1fr); }
    .kickoff-feature__poster { order: 0; }
  }

  @media (max-width: 40rem) {
    .kickoff-feature__inner { padding: var(--space-8) var(--space-5); gap: var(--space-8); }
    .kickoff-feature__poster { padding: var(--space-3); }
    .kickoff-feature__poster::before { inset: var(--space-3) calc(-1 * var(--space-3)) calc(-1 * var(--space-3)) var(--space-3); }
    .kickoff-feature__poster strong { font-size: clamp(1.5rem, 9vw, 2.5rem); }
  }

  @media (forced-colors: active) {
    .kickoff-feature,
    .kickoff-feature__poster,
    .kickoff-feature__poster::before { border-color: CanvasText; forced-color-adjust: auto; }
    .kickoff-feature::after { display: none; }
  }

  @media print {
    .kickoff-feature { color: black; background: white; border: 1px solid black; }
    .kickoff-feature::after,
    .kickoff-feature__poster { display: none; }
    .kickoff-feature .section-heading :is(h2, h3),
    .kickoff-feature__description,
    .kickoff-feature .section-heading__eyebrow { color: black; }
  }
</style>
