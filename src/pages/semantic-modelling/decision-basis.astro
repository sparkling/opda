---
import { getModellingRedesignReplacementRoute } from '@/lib/modelling-route-migrations.mjs';
const destination = getModellingRedesignReplacementRoute(Astro.url.pathname);
if (!destination) throw new Error('Missing declared modelling route replacement');
return Astro.redirect(destination + Astro.url.search, 301);
---
