{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hm.com/ns/ia/schemas/enumeration.schema.json",
  "title": "Enumeration",
  "description": "A fixed set of allowed values for a property. Represents a controlled vocabulary or code list modelled as a SKOS ConceptScheme.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Display name of the enumeration scheme."
    },
    "description": {
      "type": "string",
      "description": "Description of what this value set represents."
    },
    "values": {
      "type": "array",
      "items": { "$ref": "enumeration-value.schema.json" },
      "minItems": 1,
      "description": "The enumeration values in this scheme (1..*)."
    }
  },
  "required": ["name", "values"],
  "additionalProperties": false
}
