{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hm.com/ns/ia/schemas/suggestion-generator.schema.json",
  "title": "SuggestionGenerator",
  "description": "A data correction proposal that an application can offer to a user. Suggestions propose asserted data changes (e.g., complete a missing temporal pair, clean whitespace from a code field).",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Display name of this suggestion generator."
    },
    "category": {
      "type": "string",
      "description": "Category (e.g., temporal, cleanup, status)."
    },
    "sparqlUpdate": {
      "type": "string",
      "description": "The SPARQL UPDATE query that implements this suggestion."
    }
  },
  "required": ["name", "category", "sparqlUpdate"],
  "additionalProperties": false
}
