# OPDA — Linked Data Council Adoption Record

Per [ODR-0001 §Adoption hooks](../ODR-0001-linked-data-council-methodology.md), this record declares the project-specific context for OPDA's adoption of the Linked Data Council methodology. The methodology body itself is portable; this record fills its declared slots.

- **Project:** OPDA — Open Property Data Association linked-data programme
- **Adopting date:** 2026-05-20 (initial)
- **Methodology version:** see [ODR-0001](../ODR-0001-linked-data-council-methodology.md) frontmatter `date` field — the canonical version pointer (currently 2026-05-27)

---

## §Project Weighting

Within the standing 9, OPDA weights the following experts more heavily because the project's domain — public-interest residential-property data in England & Wales — directly engages their published methodology:

| Expert | Why OPDA weights them more |
|---|---|
| **Ian Davis** | UK-government linked-data conventions are directly relevant. OPDA produces a public-interest dataset and aspires to UK-Gov-style publishing discipline (BBC `/programmes/` model; `data.gov.uk` cookbook). Davis's publish-first / time-box framings are load-bearing for OPDA's MVP discipline. |
| **Tom Baker** | Metadata-standards and vocabulary-governance practice. OPDA's catalogue discipline (ODR-0002 Core/Conditional/Defer tiering with change-log absorption) follows the DCMI hygiene pattern Baker codified. |

This weighting is recorded in convening blocks but does NOT override panel composition — all 9 standing experts still appear in every Full Council session. The weighting affects DA selection (Davis is the natural DA for sessions where publish-first / scope-discipline framings are credible opposition).

## §Domain-Extended Panel

OPDA pre-elects these extended-panel candidates as routinely applicable because the linked-data programme genuinely turns on their expertise:

| Expert | Why pre-elected |
|---|---|
| **Holger Knublauch** | SHACL is load-bearing for `verifiedClaims`, the overlay-profile mechanism (ODR-0010), and the validation severity scheme (ODR-0013). Knublauch authored the SHACL spec and runs TopBraid customer governance — his operational opinions on profile composition and severity tiering are first-tier for OPDA. |
| **Antoine Isaac / Alistair Miles** | SKOS is the substrate for enumeration vocabularies (ODR-0011) and the per-scheme UFO meta-category declarations (Scope-Check 1 Q3 sub-finding). OPDA's 160+ JSON enums all map to SKOS schemes. |
| **Harshvardhan Pandit** | DPV is load-bearing for the governance layer (ODR-0012) and the DPV co-annotation pattern on Claims/Evidence. Pandit's session-001 dissent on lawful-basis/consent/purpose class vocabulary is a live methodological position. |
| **Renato Iannella** | ODRL is admitted in the catalogue with policy-authoring deferred. When the activation trigger fires (consent instances entering scope), Iannella's expertise becomes immediately load-bearing. |
| **Luc Moreau** | PROV-O is the backbone of Claims, Evidence & Provenance (ODR-0009). The 80%/5-residue boundary in the verifiedClaims-to-PROV-O mapping is Moreau's own analysis. |
| **Manu Sporny / Drummond Reed** | W3C VC / DID / Trust Framework interop is the deferred Phase-7 work (ODR-0016). Activates on wallet consumer or DID-method instance data entering scope. |

Other extended-panel candidates (Dehghani, Alani/Domingue, Ranganathan/ISO 25964, Evans/Vernon) remain case-by-case — convened when a specific question genuinely depends on their expertise.

## §Real-world Governance Handoff

Council verdicts shape *proposals*; adoption flows through OPDA's real-world governance:

1. **OPDA Working Groups** — domain-specific WGs (e.g. PDTF Schema WG, Trust Framework WG, AML WG) review Council proposals against operational reality and stakeholder concerns.
2. **OPDA Modelling Sub-Committee** — cross-WG body that ratifies linked-data modelling proposals into draft adopted decisions.
3. **OPDA AGM ratification** — annual general meeting where adopted decisions receive formal stakeholder endorsement.

A Council verdict is **not** a ratified OPDA decision. Records produced by Council sessions remain `proposed` until the Sub-Committee draft-adopts them; `accepted` only after AGM ratification (per project convention). The methodology's `accepted` status is necessary but not sufficient for adopted-decision status in OPDA.

## §Track Record

OPDA Council sessions, meta-Councils, and Author-only amendments convened to date. Per the methodology, every convened session — Full Council, Reduced Council, or Author-only — appears here.

| Date | Session | Format | Queen | DA | Subject | Verdict (one-line) |
|---|---|---|---|---|---|---|
| 2026-05-20 | [session-001](./session-001-pdtf-schema-to-ontology.md) | Full Council (12 voices / 6 teammates) | Elisa Kendall | Nicola Guarino | PDTF v3 schema → ontology (Q1–Q7) | Genuine modelling; concern/UFO partition; identity crux gated; spike-then-scale → ODR-0003 (anchor) + stubs ODR-0004 through ODR-0014 |
| 2026-05-26 | [scope-check-1-programme](./scope-check-1-programme.md) | Full Council — meta-Council (9 voices / 6 teammates) | Elisa Kendall | Ian Davis | Are ODR-0002…ODR-0014 the right cut? (Q1–Q8) | 8-1 APPROVE; nine amendments A1–A9 → spawn ODR-0015, name deferred ODR-0016, retire ODR-0014, DPV authoring → 0012, 0011 UFO-per-scheme, 0010↔0013 three-rule contract, defer 0008 split, six termination signals, Gandon-Guizzardi methodology gap routed to A9 |
| 2026-05-26 | [scope-check-2-hive-vs-swarm](./scope-check-2-hive-vs-swarm.md) | Reduced Council — meta-Council (6 voices / 4 teammates) | Elisa Kendall | Ian Davis | Hive-mind vs Agent fan-out: which session shapes should switch? (Q1–Q6) | 5-1 SELECTIVE; eight amendments B1–B8 → two pilots (S005 hive-mind/byzantine; S011 Q8 hive-mind/typed-output); B1 ratified later as direct ODR-0001 amendment; Byzantine framing reframed to structural vote acknowledgement; Davis Q1/Q5/Q6 dissents preserved |
| 2026-05-27 | ODR-0001 amendment (first) | Author-only (1 run; self-amendment) | Henrik (acting per the self-amendment process) | — | Make ODR-0001 portable; factor OPDA-specifics into this adoption record | Methodology body becomes project-agnostic; added citation-grounding, pre-flight scope check, format tiers, consensus-mode framework + full swarm/hive-mind config tables, working-file convention, meta-Council type, standing-panel amendment protocol, self-amendment process, adoption hooks |
| 2026-05-27 | ODR-0001 amendment (second) | Author-only (1 run; self-amendment) | Henrik (acting per the self-amendment process) | — | Add five hive-mind-advanced items to ODR-0001 | (i) Pattern lineage — Council Hive is Pattern 1; siblings out of scope. (ii) DA explicit withdraw-or-hold per contested question — silent alignment violation. (iii) Queen-composes-does-not-fabricate. (iv) One-message parallel spawn. (v) Worker failure protocol — 60s, retry-once with lineage, default-abstain |
| 2026-05-27 | ODR-0001 amendment (third) | Author-only (1 run; self-amendment) | Henrik (acting per the self-amendment process) | — | Add four hive-mind-advanced items (3, 6, 7, 8) to ODR-0001 | Cross-talk transport selection (queen-composed / SendMessage / MCP memory / file-based); multi-day session resumability (export/import/resume); sub-queen escalation for hierarchical-mesh; calling convention (Queen + sub-agents call MCP directly; SendMessage for inter-sub-agent) |
| 2026-05-27 | [session-003](./session-003-pdtf-ontology-programme.md) | Author-only (1 run; Queen synthesis) | Elisa Kendall | — | Programme operational frame for ODR-0003 — phase ordering, fast-path option, retirement criterion, status-discipline protocol, shared-question routing | ODR-0003 moves `proposed → accepted` (council: session-003); plan §5 phase order recorded as binding; plan §5.1 fast-path named as first-class alternative; plan §4.1 shared-question routing cited as single source of truth; bidirectional-update protocol (anchor + adoption-record + cross-refs) tightened. No fresh deliberation; every item plan- or precedent-sourced (Session 001, Scope-Check 1, Scope-Check 2). |
| 2026-05-27 | [session-a9](./session-a9-gandon-guizzardi-methodology-gap.md) | Reduced Council (4 runs: 2 panel teammates + 1 DA + 1 Queen synthesis) | Elisa Kendall | Nicola Guarino (DA — withdrew on all four conditions met) | Gandon-vs-Guizzardi methodology gap — what does an ODR record? Artefact-engineering decision vs ontological commitment | 2-1 BOTH-WITH-BOUNDARY at the `kind` enum (Guizzardi + Guarino DA; Gandon primary ARTEFACT-ENGINEERING with BOTH-WITH-BOUNDARY accepted as fallback). ODR-0001 amended in-place per §Self-amendment process: new §What an ODR records (per-kind discipline) subsection added to §Rules between §Citation grounding and §Cross-talk transport. For `kind: pattern \| mapping`, MUST state (a) UFO/DOLCE meta-category, (b) IC over named hard cases, (c) artefact realisation; `methodology`/`architecture`/`programme` relaxed. Gandon's artefact identity test adopted as operational supplement for `pattern` extraction. `odr-review` lint update flagged for next skill release. Pressure-test schedule: S005 + S011 are first two ratifiability tests. Gandon partial-alignment recorded; Guarino DA withdrew. |
| 2026-05-27 | [session-002](./session-002-vocabulary-catalogue.md) | Full Council (8 runs: 4 panel pairs + 1 DA + 1 Queen synthesis) | Tom Baker | Kurt Cagle (DA — withdrew on Q1, Q3–Q13; partial-withdraw on Q2) | Vocabulary Catalogue meta-discipline + absorbed-S014 codification (13 questions: 6 native + 7 absorbed) | 12 questions land 9-0 or 8-1 with full Cagle DA withdrawal (Q1, Q3, Q4, Q5, Q6, Q7, Q8, Q9, Q10, Q12, Q13); partial-withdraw on Q2. Q11 OBO RO lands DEFER 5-2-2 with Kendall + Guizzardi held-as-live positions routed to ODR-0005 follow-up. ODR-0002 amended: new `## Rules` subsections (Promotion and demotion criteria, four-condition Conditional→Core; Profile-pinning ownership, module-owner-proposes; Reference-not-import normative + three-value `adoption-mode` field); new Conditional-table columns (W3C status, Adoption mode, Profile pin); FOAF Change Log row tightened with two-reason rule-out; per-question Change Log rows Q7–Q13 codifying named-event triggers for OWL-Time demotion, DCAT promotion, SSSOM re-open, ODRL activation, cred/did activation. Pandit Q4 `dpv-pd` bundled-import divergence routed to ODR-0012 implementation. Council:session-002 set; status:proposed retained (per Scope-Check 1 Q4 — catalogue evolves through Change Log; status flips when consuming modules ratify). |
| 2026-05-27 | [session-004](./session-004-pdtf-ontology-foundation.md) | Full Council (8 runs: 4 panel pairs + 1 DA + 1 shacl-solo + 1 Queen synthesis) | Fabien Gandon | Holger Knublauch (DA — extended panel — withdrew on all 7 questions) | PDTF Ontology Foundation — Phase 1 gate (7 questions: hash/slash, layer-segregated naming, three-graph separation, term-sourcing precedence, generator-first policy, diagnostic-exemplar policy, namespace string + version scheme) | 7 questions × 9-0 vote; full Knublauch DA withdrawal on all four primary attacks (Q3 three-graph operational rule + Q4 term-sourcing five-line precedence + Q5 generator byte-identity CI + Q7 namespace-as-blocker). ODR-0004 amended: new §Operational specifications subsections 3a/6a/7a/8a — three-graph source-graphs + derived-consumer-profiles + five-part CI test (Q3); deterministic generator emission + version-pin + byte-identity CI + diff-explosion canary tests (Q5); five-line term-sourcing precedence + conflict-recording protocol + version-IRI pinning (Q4); exemplar `expected-report.ttl` pairing + parent-repo storage discipline (Q6). New Consequences: namespace-string-as-blocker on `status: accepted`; `w3id.org/opda/` recorded as named alternative for WG consideration; hash-vs-slash reopening-trigger threshold deferred to WG; `odr-review` lint specification deferred to next skill release; `opda:ValidationContext` reification routed to ODR-0010/0013 as `pattern`-extraction candidate per A9. **ODR-0004 stays `status: proposed`** until WG ratifies the namespace string (Knublauch DA primary demand: namespace decision is the gating event for the programme). Phase 1 Council gate cleared; formal Phase-1 close awaits WG namespace ratification. Session 001 Q5 carry preserved verbatim. |
| 2026-05-27 | [session-005](./session-005-property-land-identity-crux.md) | Full Council (8 runs: 1 formal-pair + 1 enterprise-pair + 1 hendler-solo + 1 governance-pair + 1 shacl-solo + 1 DA + 1 Queen synthesis); **B2 pilot — `consensus-mode: hive-mind/byzantine`** | Nicola Guarino | Dean Allemang (DA — withdrew on all 8 questions) | Property & Land: The Identity Crux — Phase 2 gate (8 questions: Endurant commitment, IC for physical Property, IC for LegalEstate/RegisteredTitle, UPRN status, 2-vs-3 class split, Address-as-mode, exemplar pass, gate clearance) | **First `kind: pattern` ODR to discharge under A9 per-kind discipline.** Q1 9-0 FOR Endurant + Substance Kind (Baker `dct:source`-to-DOLCE/UFO + Cagle machine-readable subClassOf amendments adopted); Q2 9-0 FOR spatial-material continuity with Kendall+Davis legal-record-discontinuity-override hybrid (Baker OS-AddressBase-Plus + Cagle three-SHACL-surrogates amendments adopted; Allemang DA withdrawal condition (b) met); Q3 9-0 FOR distinct ICs for LegalEstate (rights-bundle persistence) and RegisteredTitle (title-number lineage) with HMLR PG 1/16/40 + PROV-O lifecycle reification + Pandit PII-regime-distinction amendments; Q4 9-0 FOR both (SHACL/DASH operational key + PROV-O contingent identifier) + Cagle SHACL-AF succession-chain materialisation rule (Rule 6 amendment lands); Q5 **6-2-1 FOR 3-class** — Davis (2-class with `RegisteredTitle ⊑ LegalEstate` upgradeable on BASPI5-round-trip evidence) + Cagle (2-class with commonhold-spawn-rule trigger) held-as-live dissent preserved; Allemang DA withdrew on Pandit's PII-regime SHACL-discriminable case + first-registration lifecycle event; Q6 9-0 FOR routing Address modelling to ODR-0015 with minimal-constraint-recorded-here + Baker+Pandit DPV-pattern-consideration amendment; Q7 9-0 PASS with per-exemplar verdict walkthrough (registered-freehold-house + unregistered-pre-first-registration-house + flat-with-split-uprn); exemplar amendments scheduled (add LegalEstate to exemplars 1+2; canonical reified `opda:UPRNSuccessionEvent` for exemplar 3); `expected-report.ttl` pairing deferred to follow-up; Q8 9-0 FOR deliberative gate clearance with namespace block carrying. ODR-0005 amended: §Decision rewritten for 3-class; new §Operational specifications 2a/3a/3b/3c/6a/6b/7a/8a; §Consequences updated for downstream inheritance (ODR-0006/0007/0015 unblocked; **ODR-0008 deferred until cardinality crystallises** per Kendall+Davis joint amendment; ODR-0009/0010/0012/0013 inherit specific load-bearing inputs). Council:session-005 set; **status:proposed retained** per inherited ODR-0004 namespace block. Guarino S001 Q4 dissent WITHDRAWN. **B2 pilot verdict: EXTEND CAUTIOUSLY** — structured tally + retire-or-extend evaluation in transcript §B2 pilot; recommended for S015 + S011 Q8 (three-pilot threshold for EXPAND). |
| 2026-05-27 | [session-011](./session-011-enumeration-vocabularies.md) | Full Council, substrate mode (6 runs: 1 guizzardi-solo + 1 governance-pair + 1 shacl-solo + 1 DA + 1 Queen synthesis); **B3 pilot `hive-mind/typed-output` for Q8**; two-artefact discipline session-wide (third pilot site) | Antoine Isaac / Alistair Miles (extended) | Fabien Gandon (DA — 2 withdrew / 5 conceded, full DA withdrawal) | Enumeration Vocabularies — Phase 2.5 substrate (7 questions: Q3 cross-vocabulary mapping deferred to Phase-3.5 audit) | **Third `kind: pattern` ODR to discharge under A9 per-kind discipline (at the scheme level).** Q1 4-0 FOR every-enum-a-scheme + Baker steward amendment + Cagle one-primary-scheme SHACL invariant; Q2 4-0 FOR Baker SKOS-§S14/§S15 cardinality + Pandit PII-strict `definition @en` amendment; Q4 4-0 FOR ODR-0004 §7a precedence + Pandit verbatim-citation amendment for regulator-governed PII concepts; Q5 4-0 FOR three-case lifecycle discipline (Gandon DA mild attack withdrawn — `dct:isReplacedBy` / `prov:wasDerivedFrom` / `dct:modified` per case) + Cagle SHACL-AF deprecation-chain rule (re-instantiates ODR-0005 §6a; **fourth citing site of SHACL-AF non-blocking-data-quality-rules pattern**); Q6 4-0 FOR single `opda:` namespace; Q7 4-0 FOR `xsd:string` + `sh:pattern` default (custom datatypes admissible per SKOS §S15 but not minted by default); **Q8 (B3 typed-output) 4-0 FOR seven-category UFO framework** — Quale-in-Region + Role label + Phase label + Method/plan code (four original) + Quality Region + Substance Kind label + Quality Value (three Council-authored extensions; Gandon DA primary attack on source-attribution withdrawn via dual `dct:source` — upstream UFO/DOLCE + ODR-0011 SKOS-binding). ODR-0011 amended: new §Operational specifications 1a/2a/4a/5a/7a/8a discharging A9 inline; §Consequences updated with EXPAND-threshold verdict + `pattern`-extraction spawn-rule fires. Council:session-011 set; **status:proposed retained** per inherited ODR-0004 namespace block. **B3 pilot third-site EXPAND threshold satisfied** — two-artefact discipline (narrative + structured tally) becomes default across remaining sessions (S006/S007/S009/S010/S012/S013); session-level pilot labels (B2/B3 consensus-modes) retired; ODR-0001 amendment flagged for follow-up author-only session. **SHACL-AF non-blocking-data-quality-rules pattern: fourth citing site reached** (ODR-0005 §6a + ODR-0009 + ODR-0015 §4a + ODR-0011 §5a); spawn-rule fires for shared `opda-shacl-af-quality-rules` `pattern` ODR. |
| 2026-05-27 | [session-015](./session-015-address-and-geography.md) | Reduced Council (3 runs: 1 formal-pair + 1 DA + 1 Queen synthesis); **two-artefact discipline (B2-pilot second site)** | Giancarlo Guizzardi | Dean Allemang (DA — 5 withdrew / 3 conceded / **1 held-as-live on Q3**) | Address & Geography — Phase 2.6 gate (8 questions: UFO meta-category, IC, class structure, external alignment, GeoSPARQL deferral, co-reference SHACL, PII tagging, exemplar pass) | **Second `kind: pattern` ODR to discharge under A9 per-kind discipline.** Q1 3-0 FOR `opda:Address` as **UFO Substance Kind / DOLCE NonPhysicalEndurant** (revises S005 Q6 Mode-only stance on exemplar evidence + external-standards alignment); `opda:addressVariant` as UFO Quality particularising context-of-presentation. Allemang DA withdrew on 3 named consumer queries (DPV co-annotation; INSPIRE feature query; authority-succession query) + DPV class-level dispatch SHACL case. Q2 3-0 FOR five-rule IC (cosmetic-reformat / authority-succession / cross-variant-distinction / Property-side-change / INSPIRE-only-locatedness); DOLCE NonPhysicalEndurant grounding (Searle 1995). Q3 **2-1 FOR class with property shapes**; **Allemang DA held-as-live dissent preserved** with named re-open trigger ("if 18 months / zero multi-Property-shared-Address cases / no new consumer query"). Q4 3-0 FOR three alignments — INSPIRE Identifier as contingent identifier (re-instantiates ODR-0005 §6a UPRN-as-Quality pattern); `opda:Address rdfs:subClassOf vcard:Address`; OS AddressBase Plus via `dct:source`. Q5 3-0 FOR GeoSPARQL deferral with `opda:hasGeometry` interface + four named admission triggers (title-extents / LLC1 polygons / INSPIRE direct ingest / search-radius queries). Q6 3-0 FOR two-tier SHACL co-reference shape (`sh:Info` cross-variant; `sh:Warning` same-variant). Q7 3-0 FOR class-level `dpv-pd:Address` baseline + three variant-conditional refinements routed to ODR-0012. Q8 3-0 PASS with per-exemplar verdict walkthrough + two amendments scheduled (refactor exemplar 1 to resource shape; manifest exemplar 3's INSPIRE-variant). ODR-0015 amended: §Decision rewritten for Kind commitment; new §Operational specifications 2a/3a/3b/4a/5a/6a/7a/8a discharging A9; §Consequences records Allemang DA Q3 held-as-live dissent. Council:session-015 set; **status:proposed retained** per inherited ODR-0004 namespace block. **B2 pilot second site observation:** structured tally captured Q3 held dissent + DA scorecard mechanical-check as machine-readable data; supports EXTEND-CAUTIOUSLY recommendation. **`pattern`-extraction candidate flagged:** PROV-O succession pattern reaches third citing site (ODR-0005 §6a + ODR-0009 + ODR-0015 §4a); if ODR-0006 produces fourth, spawn shared `contingent-identifier-succession` `pattern` ODR per §6 spawn rule. |
| 2026-05-27 | [session-003b](./session-003b-namespace-wg-decision.md) | Author-only (1 run; Henrik acting for OPDA WG) | Henrik Pettersen | — | WG namespace ratification — `opda:` literal base URI selection from the three S004 Q7 candidates | **Phase 1 close.** WG ratified `https://w3id.org/opda/#` (W3C PICG-redirected to `https://openpropdata.org.uk/ontology/`) per Knublauch S004 DA primary demand + DPV precedent (Pandit's principal-author choice). ODR-0004 §Rules.1 amended with literal string; `status: proposed → accepted` flipped on the 13 ODRs in the `depends-on:[ODR-0004]` chain (ODR-0002/0004/0005/0006/0007/0009/0010/0011/0012/0013/0015/0017/0018). Exemplar bulk-update applied (`https://opda.uk/ns/#` → `https://w3id.org/opda/#` across 14 files in `source/03-standards/ontology/exemplars/`). Engineering realisation: [ADR-0006](../../../adr/ADR-0006-w3id-opda-ontology-namespace.md) — first cross-corpus ADR; w3id.org PR is the asynchronous engineering follow-up tracked in ADR-0006's five-criterion confirmation gate. Vestigial `council-cleared, namespace-blocked` tags removed from 6 ODRs (`status: accepted` carries the signal). **ODR-0008 stays `proposed`** (deferred on cardinality, not namespace); **ODR-0016 stays `proposed`** (deferred until trigger). |
| 2026-05-30 | [session-021](./session-021-bounded-context-implementation-plan.md) | Full Council (9 voices / 6 teammates; `agent-fan-out` + SendMessage cross-talk) | Elisa Kendall | Ian Davis | Bounded-Context Implementation Plan (Q1–Q5: membership authority; placement & coverage; missing-ontology creation; ODR/ADR scaffolding; phased plan) | **Hybrid membership** (Q1 9-0): `opda:servesContext` derived; `opda:definedInContext` **authored but generated-from-provenance** (each leaf's `dct:source`) & un-gated from the homonym trigger — curing the "partition is 100 % form-derived" inversion. Generated home-pass + total-cover CI places every entity (Q2 8-1; D1 shared-kernel / D2 scaffolding split). Execute the mechanical 935-leaf walk; placement non-blocking (Q3 9-0 non-blocking / 8-1 execute-now). **No new ODR** (Q4 8-1) — amend ODR-0019 Rule 8 + ODR-0020 Rule 4/5, revise ADR-0026, create **ADR-0028** (descriptive walk + home-pass) + **ADR-0029** (`ProfileSpec` refactor + 14 profiles). Two-track phased plan, scheme-first/shippable (Q5 8-1). **Davis DA: WITHDREW Q1/Q3-scheme/Q4/Q5-increment-1; HELD-AS-LIVE on completeness-as-a-gate** (re-open: a named consumer query the BASPI5 slice + bucket-D map cannot answer) — **OVERRULED by directing-governance directive (2026-05-30): ONE-GO delivery, full coverage, NO gates / staging / deferral / demand-triggers**. Amendments + ADRs (ADR-0028 walk + ADR-0029 profiles, executed in the same delivery as ADR-0026) stay `proposed` pending WG ratification. |
| 2026-05-30 | [session-022](./session-022-form-shacl-profile-convention.md) | Full Council (6 voices; `agent-fan-out` + SendMessage) | Tom Baker | Ian Davis | Form↔SHACL association, metadata on SHACL definitions, over-engineering check — against linked-data CONVENTION (Q1–Q4) | **Grounded in convention** (DCMI Application Profiles / Singapore Framework; W3C PROF / DCAT / conneg-by-profile; DCTAP; RDFS; SHACL; FIBO). Verdict: a PDTF form **IS a DCAP** (SHACL shapes = its DSP; the data-dictionary = a DCTAP; profile→base = `prof:isProfileOf`) — OPDA ~95% idiomatic. **Partly OVERTURNS S021:** `opda:definedInContext` **RETIRED** (6–0) — reinvents `rdfs:isDefinedBy` (module) + `dct:source` (provenance) + `dct:subject` (community); S021's "un-gate + generate-home-for-every-term" sub-vote reversed (**Queen Kendall retracted her S021 chairing**). `opda:requires` dropped (shapes-redundant); `overlaysContext` **dropped** (not re-pointed). **Governance directive (2026-05-30) OVERRULED any PROF / profile-object layer** — no `prof:Profile`, no `prof:isProfileOf`, no spike: the SHACL overlay IS the form (ODR-0010); form↔base is structural `sh:targetClass`; form↔community = a `dct:subject`/`dct:publisher` triple on the form graph. **KEEP:** SKOS 6-context scheme, `servesContext` (dormant rule), `consumesFrom`, per-shape `dct:source`, F1 firewall, 935-leaf walk, 14-profile rollout (re-framed DCTAP→SHACL). **DROP:** S021 cross-check shape + total-cover-CI + F2/F3. Davis: Q3c WITHDRAWN (3 Recs, unconditional); `servesContext` + one-go-bundling held-as-live. Amendments stay `proposed` pending WG. |
| 2026-05-30 | ODR-0001 amendment (fourth) | Author-only (1 run; self-amendment) | Henrik (acting per the self-amendment process) | — | Promote `SendMessage` via Agent Teams to the recommended default cross-talk transport; demote queen-composed to exceptional fallback | §Cross-talk transport reordered — **`SendMessage` via Agent Teams (recommended default)** → MCP `_memory` (fallback) → file-based (fallback) → queen-composed (exceptional, forgoes cross-talk). Ratifies the convention S021 + S022 used consistently: direct agent-to-agent dialectic (teams `council-021`/`council-022`, one opening + one rebuttal pass) is the point of the Council; queen-composed negates it. **Queen *role* unchanged** (scope check / framing / synthesis); only the discussion transport changes. §Substrate operations calling-convention also updated to name SendMessage as the default. AgentDB re-index (`odr-index`) pending. |
| 2026-05-30 | [session-023](./session-023-descriptive-layer-import-strategy.md) | Full Council (11 voices / 8 teammates; `agent-fan-out` + SendMessage team `council-023`) | Dean Allemang | Ian Davis | Descriptive-Layer Import Strategy & Property Categorisation (Q1–Q6: diagnosis; A–G taxonomy; whole-vs-part; micro-patterns; checklist+enums; coverage/round-trip+residual) | **Import the descriptive layer BY CATEGORY, NOT the 1:1 mechanical 935-leaf walk** (Q1/Q3/Q4/Q5/Q6 **11–0–0**; Q2 10–0–1). Empirical: of ~1,493 annotated base leaves only **~181 (~12%)** are genuine descriptive concepts; ~88% are form-ergonomics + repeated micro-structure (56% are ~16 generic tails; 378 enums → 54 value-sets; `yesNo`×1,135 per `audit.json`). **Seven-category taxonomy (A–G)** with fixed treatments: A disclosure tails → one `opda:disclosureDetail`; B evidence → reuse ODR-0009; C 54 status enums → ODR-0011 SKOS schemes; D fixtures checklist (≈89 items) → `FixtureItemScheme` + 3 props (inclusion = **transaction Mode**, not Property Quality); E search/risk results → `SearchResult`/`RiskAssessment` class + peril scheme (**fires ODR-0008 §Q2a(b) spawn**); F address/contact/geo → reuse ODR-0015/0006; G ~181 → curated per-leaf walk + **regulatory-salience allow-list**. **Davis DA: WITHDREW the mechanical-walk fight + narrowed the S021 completeness dissent → converted into three ratification gates** — **G1** path-aware binning (DA-verified `priceInformation.price` mis-bin), **G2** schema-leaf-path `dct:source` (DA/Kendall-verified: current emission points at the ODR record, not the leaf), **G3** coverage-by-test (BASPI5 round-trip on the *collapsed* TBox + worked per-leaf query) — withdraws in full on G1+G2+G3 passing. Kendall traceability+salience gate adopted. → **[ODR-0022](../ODR-0022-descriptive-layer-import-strategy.md)** (`architecture`, proposed). **Re-scopes ADR-0028** walk to G+salience; **ADR-0029** profiles must enumerate per-form leaves. ~80% reduction in the WG per-leaf operation. Pending WG ratification. |
| 2026-05-30 | [session-024](./session-024-authority-retrieved-artefacts.md) | Full Council (lean; 8 voices / 5 teammates; `agent-fan-out`, **no hive-mind**) | Elisa Kendall | Kurt Cagle | R1 — Authority-Retrieved Artefacts (RiskAssessment class + peril scheme; Q1–Q6) | **7–1 / 8–0 FOR `opda:RiskAssessment` as a UFO Information Object** (⊑ `prov:Entity`; IC ⟨generating activity, peril, subject, generated-time⟩) + a **12-member `opda:PerilScheme`** SKOS scheme (steward Baker); the five §Q4a classes **retro-corrected Substance-Kind → Information Object**; whole family on ODR-0009 PROV-O (`datasetAttribution` ≡ `prov:wasAttributedTo`); one class for env + CON29; self-referential `opda:hasSubAssessment`. **Cagle DA HELD-AS-LIVE for alt-d (reuse `opda:Search`)** — re-open trigger: real data shows no `RiskAssessment` has a lifecycle independent of its parent `Search`; the re-run hard case meets his withdrawal trigger. → **[ODR-0008d](../ODR-0008d-authority-retrieved-artefacts.md)** (`pattern`, proposed). First of the R1–R4 parallel run; unblocks ODR-0022 Category E. |
| 2026-05-30 | [session-025](./session-025-ufo-axis-submodules.md) | Reduced Council (provisional; 3 voices; `agent-fan-out`) | Giancarlo Guizzardi | Ian Davis | R2 — UFO-axis sub-modules (split Category-G into ODR-0008a/b/c?) | **Ratify the 3-axis split FRAMEWORK + stewards + per-axis load-bearing trigger; mint NOTHING; defer the a/b/c spawn** to the curated G walk (Allemang + Davis aligned). Davis DA HELD (§Q2a(a) trigger unfired — the directing-authority override discharged *convening*, not the substantive gate). **Surfaced a binning-rule defect** — enum-bearing Quale-in-Region G-concepts (`currentEnergyRating`/`councilTaxBand`/`builtForm`/`tenureKind`) mis-routed to Category C; fix logged against ODR-0022 §1 / ADR-0030 §G1. Provisional under gate-override; no new record; ODR-0023 R2 stays gated. |
| 2026-05-30 | [session-026](./session-026-building-room-promotion.md) | Reduced Council (provisional; 3 voices; `agent-fan-out`) | Henrik (convening) | Ian Davis | R3 — `opda:Building` / `opda:Room` class promotion | **HOLD — defer; a deferral, NOT a denial.** Guarino: the eventual IC is genuine (+O; ODR-0005 §3a "Replacement" witness) but not consumer-exercised; trigger **sharpened to re-identification** (a query that re-identifies a Room/Building instance, not one that merely mentions it); interim facts stay datatype on `opda:Property`; dormant `skos:exactMatch`→subclass path preserved at zero cost. Davis DA + Guarino aligned — **no held-as-live dissent**. Provisional under gate-override; no new record; ODR-0023 R3 stays gated. |
| 2026-05-30 | [session-027](./session-027-fixtures-inclusion-mode.md) | Author-only (1 run; no panel; `consensus-mode: none`) | Henrik (convening) | — | R4 — fixtures inclusion-as-transaction-Mode | **CONFIRMED:** `opda:inclusionStatus` is a UFO Mode/Relator of the *sale transaction* (ODR-0007), not a Quality of `opda:Property` (the same boiler is included in one sale, absent the next). Confirms ODR-0022 §4; the Relator IC is routed to ODR-0007; **Category-D emission unblocked**. No new record; ODR-0023 R4 struck. |
| 2026-05-31 | [session-028](./session-028-category-g-walk-review.md) | Full panel (5 voices; `agent-fan-out`, no hive-mind; greenfield review) | Dean Allemang | Jim Hendler | Curated Category-G walk review — 11 questions + mappings (facility classes, Room, monetary, free-text, domain-less, titleNumber, document/evidence, G11, bank-account, binning, datatype-vs-SKOS) | **Ratify the walk's mechanical spine; two DA blockers + several revises** → **[ODR-0024](../ODR-0024-curated-category-g-walk-dispositions.md)** (R1–R12, `pattern`) + **[ADR-0032](../../../adr/ADR-0032-category-g-walk-emission-and-coverage-gate.md)** (engineering + `ci-dup-declaration`/`ci-category-g-coverage`). Blockers: re-warrant `opda:NearbyFacility` off the borrowed §Q4a cite (UFO Substance-Kind; collapse School/HealthCareFacility subkinds — Guizzardi held dissent) + withdraw the 18 monetary false-collapses onto fixtures `opda:price` → defer to the `MonetaryAmount` monetary walk. R5 structural C-vs-G rule grew candidate-G 188→239; honest coverage **185→179/239**. Greenfield, council is the ratifying body. |
| 2026-05-31 | [session-029](./session-029-r2-ufo-axis-load-bearing.md) | Full Council (6 voices; `agent-fan-out`, no hive-mind; file-based positions) | Dean Allemang | Ian Davis | R2 reconvened — is the UFO-axis split (ODR-0008a/b/c) operationally load-bearing now the curated G walk has landed? (Q1–Q5) | **§Q2a(a) judged NOT load-bearing (0–6–0 on Q1–Q4); do NOT spawn; ODR-0008 stays monolithic; mint nothing.** Decisive finding: the walk typed leaves by **bearer Kind (`rdfs:domain`), not UFO meta-category** — the presupposed UFO-typed partition was never built — and `priceQualifier`/`ownershipType` fail OntoClean disjointness for a module boundary (Guarino: rigid *typing* ≠ sound *partition backbone*). **ODR-0008c struck from the UFO axis** (= `rdfs:domain opda:LegalEstate` = Kendall's attachment-class four-way, re-routed to its encumbrance-cardinality trigger); **a/b deferred** on a sharpened conjunctive trigger (committed gUFO typing pass *with straddlers adjudicated* AND a biting query/validator). UFO insight preserved as a deferred **gated gUFO `rdf:type` annotation pass** (uncontested leaves, annotation graph) — itself conjunct (i) of the trigger (Q5 6–0–0). DA Davis's case *became* the verdict (sustained, no condition met). Framework (S025) affirmed; no new ODR — amends ODR-0023 R2 + ODR-0008 §Q2a(a), re-routes ODR-0024 R4. |
| 2026-05-31 | [session-030](./session-030-room-building-modelling.md) | Full Council (8 voices; `agent-fan-out`, no hive-mind) | Elisa Kendall | Kurt Cagle | R3 reconvened (directing-authority override) — should `opda:Room` / `opda:Building` be classes, a by-value structure, or flat properties? (Q1–Q5) | **Mint NO Kinds: `opda:Room` 8–0 (no data-realisable IC — `roomName` non-rigid, no positional token); `opda:Building` 7–1 (genuine +O IC via ODR-0005 §3a-4 Replacement, but LATENT/unexercised → inert empty Kind).** Model the `roomDimensions.rooms[]` repeating group as an anonymous by-value **`opda:RoomDimension`** structure (no IC/key — the ODR-0024 R3 `opda:MonetaryAmount` pattern) on `opda:Property` via `opda:hasRoomDimension`: `length`/`width` (`xsd:decimal`, metres-by-comment, NO `UnitOfLengthScheme` — unit-scheme 3–5), `roomName` (`xsd:string`, non-rigid, never a key); no mereology, no transitivity, keyless shape. DA Cagle's "structured value, not a class" *became* the verdict (conceded Building's IC genuine). **No new ODR/ADR** — amends ODR-0024 R10 + ODR-0023 R3 (struck) + ADR-0005 §G24 + ODR-0008 §Q4a. Held dissents (re-open on an identity fact): thin Building bearer (Kendall), `UnitOfLengthScheme` (Guizzardi/Davis/Guarino). Emission **executed via [ADR-0033](../../../adr/ADR-0033-room-dimension-value-structure-emission.md)** (239/239 — curated Category-G walk complete; all 6 gates green, 266 tests). |
| 2026-05-31 | [session-032](./session-032-status-scheme-grain.md) | Reduced→Full Council (5 voices; `agent-fan-out`, no hive-mind) | Guizzardi (lead) | Elisa Kendall | ODR-0007 status-machine grain — one status state-machine across the transaction + all roles, vs per-role? (Q1–Q3) | **Single scheme — AFFIRM 5–0–0 on all three; DA Kendall FULLY WITHDREW (clean full-withdrawal session).** The four envelope states (`Proposed`/`Invited`/`Active`/`Removed`) are the whole participant-status surface — no role-specific lifecycle data; the milestone straddler test also fails. Refinement adopted: **two distinct Phase-bearers** (milestone-Phase → `opda:Transaction`; participantStatus-Phase → the participant role-play). **No new ODR** — resolves ODR-0007 §Rules "single-scheme-vs-per-role (OPEN)" + **lifts its TBox freeze gate**; ODR-0011 records one `ParticipantStatusScheme` + one `MilestoneScheme` (role views via `skos:Collection`, never per-role) + the SET-test re-open trigger. |
| 2026-05-31 | [session-033](./session-033-governance-class-vocab.md) | Full Council (5 voices; `agent-fan-out`, no hive-mind) | convening orchestrator | Dean Allemang | ODR-0012 lawful-basis/consent/purpose class vocabulary — adopt-now (TBox) vs Phase-2? (Q1–Q3) | **REVISE / reconcile (per-question 3–2; DA Allemang HELD-as-live — NOT a clean session).** Pre-flight found a **stale-record defect**: session-012 Q2 already ruled the lawful-basis layer 10-0 (reference-not-import; Pandit vindicated) while ODR-0012's body still read "open / not frozen". Allemang's decisive (verified) point: reference-not-import yields references + SHACL, **not** a model-constraining lattice (that needs `owl:import DPV`, out of scope). **No new ODR** — reconciles ODR-0012 §Live-question/§Consequences; consent/policy *instances* + the import-lattice → Phase-2; **`opda:PurposeScheme` model ratified** (ODR-0011 §8a, **emission gated on its first driver**); **3 verified emission defects logged → [ADR-0005](../../../adr/ADR-0005-deferred-work-register.md) §G / plan B4** (D1 namespace bug, D2 `opda:lawfulBasis` overload, D3 `opda:isPIIBearing` never emitted → Phase-1 PII enforcement inert). |
| 2026-06-01 | [session-034](./session-034-overlay-leaf-enumeration-discipline.md) | Full Council (6 voices; `agent-fan-out`, no hive-mind; file-based positions) | Holger Knublauch | Ian Davis | Overlay-profile leaf-enumeration discipline — B1 / ADR-0029 gap-1 completion (Q1 eager-vs-consumer-gated; Q2 ref-less forms; Q3 resolver mechanism; Q4 coverage honesty) | **REVISE — carries as eager-on-bindable + gap register.** Scope-fenced: the leaf→category mappings (ODR-0022/0024/0008d) were settled and untouched; S034 ruled only the un-councilled enumeration layer. **Q1 (3 AFFIRM / 3 REVISE, converging) + Q4 (6–0):** "full coverage" REDEFINED to *full coverage of the **bindable** set + an honest emitted per-form gap register*; the ADR-0029 one-go ↔ ODR-0021 YAGNI tension **dissolved** — enumerating a form's own leaves IS the SHACL overlay (not a fenced wrapper) and activates already-committed terms (Kendall: mints no IRI → YAGNI doesn't bite). Enumeration does NOT discharge G3's worked-query limb (per-consumer gate). **Q3 (5–1):** ratify the corpus-driven **bind-only-what-exists** resolver (emitted predicate + single `rdfs:domain` → bind; else GAP; never fabricate/guess; one ref → one `sh:path`) — Guarino: a guessed domain asserts a false inherence — **conditional on 7 verified as-built fixes** (broken import; `ntsRef` extension key; G3 authority prefix; intrinsic path-awareness; 3-branch tests; wiring; GAP-emits-no-ref). **Q2 (anchor 5–1; oc1/llc1-as-forms 2–4):** a JSON-pointer schema-leaf-path is a valid ODR-0022 G2 `dct:source` anchor (cures the live oc1/llc1 deciding-ADR anti-pattern); but oc1/llc1 stay **THIN** — they are ODR-0008d authority-retrieved register extracts, not forms. **DA Davis: WITHDRAWN Q1/Q3, CONCEDED Q4, HELD Q2** (oc1/llc1; re-open on a register-data consumer query). **No new ODR** — amends ADR-0029, ODR-0022 G2, ODR-0008d, ODR-0021. Greenfield, no WG; status proposed. Plan B1. |
| 2026-06-01 | [session-035](./session-035-evidence-alias-retirement-and-faceted-typing.md) | Full Council (8 voices / 7 teammates; `agent-fan-out` + **SendMessage cross-talk** team `council-035`, no hive-mind) | Elisa Kendall | Ian Davis | Evidence-class `owl:equivalentClass` short/long alias + faceted-vs-inheritance typing (Q1 alias anti-pattern; Q2 subclass-vs-facet; Q3 disposition) | **REVISE — retire the alias; keep the subclasses; wire the orphaned scheme. All three Q's 8–0–0** on a *heavily amended* proposition (the literal "facet **instead of** inheritance" remedy drew zero support). **Q1:** retire the 3 aliases → `skos:altLabel` + `skos:exactMatch` (synonym-as-class anti-pattern; inert under ODR-0025/0026 yet a latent DL hazard; an **active silent-pass** today — exemplars typed `a opda:Document` match no emitted shape). **Q2:** "facet vs inheritance" is a **false dichotomy** (SKOS §3.5.1 — a resource may be both `skos:Concept` and `owl:Class`); adopt the **dual layer** — keep the `…Evidence` OWL subclasses as structure-bearers (they partition by provenance *origin* = rigid, not anti-rigid) **AND** wire the **already-ratified-but-orphaned `opda:EvidenceMethodScheme`** (rename → evidence-**kind**, `ufoCategory "Substance Kind label"`) as a governed facet, `skos:exactMatch`-bound; recast `opda:Evidence` as an anti-rigid **RoleMixin** founded by the latent `opda:Verification` Relator (ODR-0006 precedent); **re-sort `VouchEvidence` to an Agent-founded attestation Relator** (the whole panel converged: it is categorially ≠ the document evidences — *why* ODR-0009 R5 "do NOT collapse" is right). **Q3:** staged, near-free migration (3 alias deletions + 3 exemplar re-types + scheme rename + 3 `skos:exactMatch` + 3 DPV-record re-points + emit the long-unbuilt per-subtype value-keyed dispatch; **zero existing-shape edits**). *(session-036 realised that dispatch as a `sh:or` material implication, not `sh:qualifiedValueShape`.)* **DA Davis: WITHDRAWN Q1** (verified the cost is 3 hand-authored lines) **+ Q3 alias-retirement** (the orphaned scheme IS the demand-pull, clearing the ODR-0026 bar); **HELD** the facet-must-not-replace-the-subclass-dispatch line (re-open: a named uniform-evidence consumer query, or any move to `sh:xone`-over-`evidenceType`). **Findings (route regardless):** orphaned+mislabelled `EvidenceMethodScheme`; ODR-0009 per-subtype `sh:xone` never emitted; `opda:Evidence` comment overclaims it. **No new ODR** — routes ADR-0011 (retire alias), ODR-0009 (RoleMixin recast + Vouch re-sort + emit dispatch), ADR-0012 (emit shape), ODR-0026 §R2/§R3 (drop the 3 `equivalentClass` axioms). Greenfield; status proposed pending WG. |
| 2026-06-01 | [session-036](./session-036-classification-over-inheritance.md) | Full Council (8 voices / 8 teammates; `agent-fan-out` + **SendMessage cross-talk** team `council-036`, no hive-mind) | Elisa Kendall | Giancarlo Guizzardi | Classification vs inheritance — directing-authority directive *"always prefer classification over inheritance where you can"*; re-examines session-035's keep-the-subclasses outcome (Q1 the principle + test; Q2 evidence application; Q3 disposition) | **REVISE — adopt the directive as a *tested default*, not "always"; AFFIRM in-tree session-035 (no collapse); re-key enforcement to the value. All three Q's 8–0–0.** **Q1:** codify the **OntoClean load-bearing cascade** in **ODR-0011 §8a** (not a new ODR — unanimous siting): −R → classify; +R∧−I → classify (the directive's largest target — `tenureKind`/`ownerType`); +R∧+I∧+D → Relator; +R∧+I∧−D → subclass. **Rigidity gates; +I (latent or emitted) warrants; Δ is evidence of +I, not a substitute (thin-Δ ≠ −I).** Directive is already opda's de-facto law (`tenureKind`/`ownerType` are coded facets, no subclass trees). **Q2:** REJECT the collapse — keep all three evidence subclasses per the cascade: Vouch (cell-3 Relator, `attestedBy`), Document (cell-4 `⊑ AttachedDocument` bearer-IC), ElectronicRecord (cell-4 **+I latent / realisation-incomplete** — retained documentary per the `opda:Building` ODR-0024 §R10 precedent; a latent IC is not inertness → NOT collapsed; trigger = EMIT `record.source.name`, never delete). **Q3:** AFFIRM session-035; **re-key enforcement VALUE-keyed not class-keyed** (Knublauch + DA Guizzardi = the directive's enforcement half) — emit `opda:EvidenceFacetShape` (`sh:targetSubjectsOf opda:evidenceType` + `sh:or(¬P∨Q)` material implication: Vouch ⇒ `attestedBy` a `prov:Agent`; SHACL-Core, entailment-free), replacing the entailment-relative `sh:targetClass VouchEvidenceShape`; add `opda:*CoherenceShape` (the ONE principled class-keyed shape: class ⇒ matching code); **fix two "asserts-more-than-it-performs" findings** — the `tenureKind`/§8a dangling subclass-binding (keystone) and the phantom `EvidenceFacetShape` comment; close the scheme-IRI rename as **no-op/withhold-permanently** (Cool-URIs). **DA Guizzardi WITHDREW** his conjunctive-AND test for Guarino's cascade + contributed the coherence shape. **Implemented + green:** 7 CI gates · 337 pytest · 27 round-trip · byte-identity. **No new ODR** — amends ODR-0011 §8a (cascade), ODR-0009 §R5 (graduated audit + value-keyed enforcement + scheme-rename close), ADR-0012 (the shapes); ODR-0026 unchanged. Greenfield; codify-don't-relitigate. |
| 2026-06-01 | [ODR-0027](../ODR-0027-classification-roles-inheritance-skos-doctrine.md) (directing-authority decision; no panel) | Author/directing-authority (no council) | — | Adopt the hm semantic-modelling classification/roles/inheritance/SKOS doctrine wholesale; record it completely | **ACCEPTED (directing-authority).** New `architecture` ODR recording the doctrine adopted from hm ODR-0010/0014/0016/0023/0025/0026/0092: **R1** classification = `isMemberOf` (coded SKOS, default) OR `isA` (subclass — genuine Kinds w/ own IC only; the ODR-0011 §8a cascade is the `isA`-admission test); **R2** a *facet* = type-borne attributes (presupposes the type; does NOT classify — `evidenceType` is an `isMemberOf` classifier, not a facet; Ranganathan/hm "facet=axis" overload flagged); **R3** Roles anti-rigid → NEVER `rdfs:subClassOf` a Kind (hm fixed 13 Role-as-subclass errors); **R4** every enum/axis = `skos:ConceptScheme` coded value, never a subclass tree; **R5** domain/range documentary, SHACL enforces, enforcement value-keyed; **R6** SUPERSEDES session-036's keep-the-`…Evidence`-subclasses disposition — evidence is a role a document plays → coded classification; `opda:AttachedDocument` stays as the genuine Kind. Reflected into ODR-0011 §8a, ODR-0009, ADR-0011/0012. **Evidence emitter re-model IMPLEMENTED (2026-06-01):** the 3 `…Evidence` subclasses retired → `opda:evidenceType` coded classification; `opda:AttachedDocument` kept as the genuine Kind; `opda:Evidence` a RoleMixin; `attestedBy` re-homed to `opda:Evidence`; `*CoherenceShape`s dropped; DPV records value-keyed; exemplars re-typed. All 7 CI gates + 337 pytest + 27 round-trip green; byte-identity re-pinned. |
| 2026-06-01 | [session-037](./session-037-url-scheme.md) | Full Council (7 voices; `agent-fan-out` + SendMessage cross-talk team `council-036`, no hive-mind) | Elisa Kendall | Ian Davis | The entire `opda/pdtf` URL scheme vs best practice + hm/DPV prior art (Q1 org/standard base; Q2 hash-vs-slash; Q3 versioning; Q4 flat-vs-modular; Q5 standard-vs-physical split; Q6 data-dictionary; Q7 SHACL shapes + `/shacl/` proposal; Q8 profiles) | **REVISE — amends [ADR-0006](../../../adr/ADR-0006-w3id-opda-ontology-namespace.md).** Decisive criterion (Baker, all voices): **governance/identity boundaries in the URI; logical/serialisation/version distinctions in triples — never in the string.** **AFFIRM:** base `…/opda/pdtf/` (Q1 7-0-0); version-out-of-IRI (Q3 7-0-0); flat namespace (Q4 6-1-0); standard/physical distinction + data-dictionary→harness (Q5 6-1-0 / Q6 6-0-1). **Amendments (final, post cross-talk):** **Q2 (5H-2S) — council RECOMMENDS REVERTING TO HASH** (Option H `…/opda/pdtf#Property`; Gandon+Baker+Pandit+Davis): the slash override **fails ODR-0004's own reopening trigger** (≥1,000 terms / named consumer — both unmet) and DPV/hm are slash-to-module+hash-within counter-precedents — **this contradicts the directing-authority no-hash directive; operator ratifies** (accept hash, or keep slash WITH a per-term 303/conneg build); Q3 add `owl:versionIRI` + dated `/harness/release/` snapshot (OWL 2 §3.1); Q4 (7-0-0) flat + unified reopening criterion (segment only on independent dereference/version/import); Q5 (7-0-0) **Baker's binding placement procedure** (citation test + harness-default + promote-by-council + never-demote + one-directional dependency), `/harness/` a **path under ONE `/opda/` redirect, not two coordinate namespaces**; **Q7 (7-0-0) `/shacl/` REJECTED** — shape nodes → `/pdtf/`, documents → `/harness/` (on authorship/part-of, not meta-shape coverage); **Q8 disaggregate + WARRANTY test** (Pandit) — documents/fixtures → `/harness/`, default the 31 form overlays → `/harness/profiles/`, promote on warranted adoption. **DA Davis:** WITHDRAWN Q1/Q3/Q4/Q5/Q6, CONCEDED Q7, **HELD only Q2 (hash — now the majority view)**; his attacks forced the two most durable artefacts (Baker's Q5 placement procedure; Pandit's Q4 reopening criterion). **Framing correction:** DPV is a slash-module+hash counter-precedent, not an endorsement of flat-slash. **⚠ TWO open operator calls: (Q2)** accept the council's hash recommendation (revert to `…/pdtf#Property`) or override it and keep slash with a per-term resolution build; **(Q8)** are profiles published AS normative PDTF (→ `/pdtf/profiles/`) or mapping-views onto external forms (→ `/harness/profiles/`, the default)? Greenfield; status proposed pending operator ratification of Q2 + Q8; blocks the namespace migration. |
| 2026-06-14 | [session-038](./session-038-ontology-reference-document-outline.md) | Full Council (6 voices / 6 teammates; `agent-fan-out`, no hive-mind) | Dean Allemang | Silvio Peroni (extended — LODE/Widoco/pyLODE) | Single-page ontology reference document — outline + soundness/completeness review (Q1–Q6) | **REVISE — a single-page HTML *delivery surface* is right ONLY as a hand-authored narrative shell + a build-time-injected generated per-term body emitted by `opda-gen` under a doc-drift CI gate; reject the hand-typed "every aspect" monolith + reject fragmented Widoco per-term pages.** Q1 REVISE 2-4-0 (definition-before-constraint; the UFO frame leads) · Q2 REVISE 1-4-1 · Q3 REVISE 1-3-2 · Q4 REVISE 0-5-1 · Q5 AFFIRM 4-2-0 · Q6 AFFIRM 4-2-0. **DA Peroni WITHDREW Q2/Q3/Q4** — the council adopts his generate-not-hand-author re-open trigger as the binding production model. Engineering realisation → **[ADR-0041](../../../adr/ADR-0041-ontology-reference-document-generation.md)**. Grounded pre-flight: the non-conformant baspi5 expected-report reads `sh:conforms true` (SHACL-AF-only violation surface); ODR-0004 prose still cites the stale `w3id.org/opda/#` base (the artefact is correct, the ODR drifted). Status proposed. |
| 2026-06-14 | [session-039](./session-039-entailment-regime-and-epc-modelling.md) | Full Council (6 voices / 6 teammates; `agent-fan-out`, no hive-mind) | Dean Allemang | Jim Hendler | Entailment-regime smell + the EPC-certificate "modelling error" (Q1–Q4) | **REVISE — the operator's suspicion is directionally vindicated but RE-LOCATED.** The core model is already UFO-correct (do **not** re-home `currentEnergyRating` onto the certificate, nor add a Property-side `recordsRating`); the bespoke entailment subset is **logically sound (keep)** but **mislabelled** (not OWL 2 RL — rename `opda-owl-rl-safe`) and **over-framed** (only 1 of 7 rules ever fires — reframe minimal/dormant, keep the negative gate). Real fixes: a **SHACL domain-as-constraint layer** (Q4 AFFIRM 6-0), the ADR-0014 round-trip `inference="rdfs"` flag, the dangling empty `Baspi5_EPCCertificateShape`, ODR-0028 R3's wording. Q1 REVISE 3-3 (locus corrected) · Q2 REJECT-smell 3 / REVISE-rename 3 · Q3 SHRINK 4-2-0. **DA Hendler WITHDREW** (the mechanism is kept, not amputated — his red line) + **CONCEDED** the rename. Routes → ODR-0025 §R1, ODR-0028 R3, ADR-0035 (reframe), ADR-0014, a new domain/range-as-SHACL `pattern` ODR + code/config. Status proposed. |
| 2026-06-14 | [session-040](./session-040-foundational-ontology-choice.md) | Full Council (6 voices / 6 teammates; `agent-fan-out` + live SendMessage team `council-foundational-ontology`, no hive-mind) | Dean Allemang | Kurt Cagle | The foundational-ontology choice — retain UFO-as-lens / deepen to OntoUML / switch to BFO / drop? (Q1–Q4) | **AFFIRM (scoped) → produces [ODR-0030](../ODR-0030-foundational-ontology-choice.md).** Retain UFO as a **design-time lens scoped to the Relator/Role/RoleMixin spine** (Q1 6-0 — UFO is not "grounding the model"; OntoClean carries the rigidity/identity calls; the 35 register-deference quality codes drop the UFO byline); do **NOT** deepen to OntoUML/full-UFO (Q2 6-0 — the gUFO authors' OWL-2-DL undecidability result is decisive); **LAYER** the standards alignment — ADOPT-NOW an IAO information-artefact crosswalk, HOLD-IN-RESERVE the BFO spine + ISO-21838 credential, deontic core stays UFO-L (Q3 6-0); ship the **honesty dispositions** — "UFO-informed" not "UFO-grounded", name the DOLCE / WonderWeb-D18 lineage in a `skos:scopeNote`, flag UFO-C least-mature (Q4 6-0). Panel: Guizzardi, Guarino, Knublauch, Barry Smith. **DA Cagle WITHDREW** his REJECT push with three live re-open triggers (the ufoCategory layer ever made reasoned-over; the 35/47 DOLCE-under-UFO-label mismatch causes a downstream error; the foundation mapping blocks a council). Status proposed. *(Rule 1 + Rule 7(b) later found breached in the committed corpus → enforced by [session-041](./session-041-ufocategory-upper-ontology-representation.md) / ODR-0031 / ADR-0045.)* |
| 2026-06-15 | [session-041](./session-041-ufocategory-upper-ontology-representation.md) | Full Council (6 voices; `agent-fan-out` + SendMessage team `council-foundational-ontology`, no hive-mind) | Dean Allemang | Kurt Cagle | `opda:ufoCategory` + upper-ontology-layer representation (Q1 string-vs-resource; Q2 markup+graph-placement; Q3 web-pages/IA; Q4 what-else-to-mark-up) | **REVISE — enrich by REFERENCE + DISCLOSURE under a RESTORED quarantine, not by minting-into-the-reasoned-graph → produces [ODR-0031](../ODR-0031-ufocategory-upper-ontology-representation.md) (proposed).** Load-bearing finding (verified ×4 at file:line): **ODR-0030 Rule 1 breached in shipped bytes** — all ~87 `ufoCategory` triples + the declaration sit in the reasoned-union graphs, `opda-annotations.ttl` carries zero, and `three_graph_test.py` has no check for it (Cagle's S040 re-open trigger (i) has fired). **Q1 4-2-0 FOR resource** — local SKOS concept in `opda:UFOCategoryScheme`, `skos:closeMatch`→gUFO, splitting the predicate (the 47 register-deference scheme tags → string/separate predicate per ODR-0030 Rule 2); Queen Allemang holds for direct-gUFO-reference, Cagle (DA) HELD for governed-string+`sh:in`. **Q2 5-1-0 → 6-0 on landing** — retype `owl:AnnotationProperty` (inert by OWL 2 §10.1), relocate all tags+declaration to `opda-annotations.ttl`, add a 6th three-graph CI gate; `sh:in` as a tag-only editorial guard, never in the instance-validation union; **the precondition for all enrichment** (Cagle WITHDREW, condition met). **Q3 5-0-1 FOR** — pages as read-side SPARQL projections (reversible); **ship the missing ODR-0030 Rule-7(b) DOLCE disclosure** (verified absent on the predicate); lead with "UFO-informed, not UFO-grounded"; badges "classified-under" not "is-a". **Q4 4-2-0 per-candidate triage** — mark up the IAO crosswalk (Rule 4) + bounded 9 gUFO `closeMatch`; keep gUFO `rdf:type` markers as-is (audit clean); decline Relator-founds (already topology) + UFO-L correlativity (SHACL/own-ODR). **HELD-AS-LIVE 3-3 split: OntoClean meta-property markup** (FOR Guarino/Guizzardi/Cagle-withdrawn as "separability insurance"; AGAINST Allemang/Baker/Gandon as "methodology-not-artefact") → routed to a follow-up Reduced Council / operator decision. Cagle held-as-live (S040, sharpened): if enrichment ships without the Q2 quarantine restoration → revert to Option D. Greenfield; status proposed; operator ratifies. |
| 2026-06-15 | [session-042](./session-042-ontoclean-meta-property-markup.md) | Reduced Council (4 voices; `agent-fan-out`, file-based + queen-composed) | Elisa Kendall | Tom Baker | OntoClean meta-property markup — the session-041 Q4 held 3–3 (Q1 mark-up-vs-prose; Q2 representation+scope; Q3 the canonical-check gate; Q4 disposition) | **REVISE → conditional adoption — the 3–3 converged onto one condition.** Mark up the OntoClean meta-properties (**±R/±I floor, +±D where a Relator decision turned on it, never ±U**) as `owl:AnnotationProperty`, `sh:in`-governed, per-type, scoped to **the subsumption lattice the check ranges over + its contrast set** — **iff** the canonical OntoClean check (every −R type that nonetheless `rdfs:subClassOf`-subsumes; IC-incompatible subsumption) ships **atomically** as a TBox-only CI meta-shape (the ODR-0031 R3 tag-guard pattern, never instance-keyed). **Decisive finding (verified ×3): the per-category OntoClean signature ALREADY ships** in ADR-0045's `UFOCategoryScheme` `skos:definition`s (4 of 9; prose; no unity), and **no CI gate reads the per-type vector today** — so the question reduced to "also per-*type*, for a machine," answered "yes iff a machine reads it." **All four converged (Q1–Q4 4–0 on the gated disposition); DA Baker WITHDREW** (his withdrawal condition — "the check ships as a running CI gate" — IS the adopted atomic rule); held-as-live REJECT only if tags ship without the gate. Resolves **[ODR-0031](../ODR-0031-ufocategory-upper-ontology-representation.md) §R7(a)**; engineering → **[ADR-0046](../../adr/ADR-0046-ontoclean-meta-property-markup.md)** (proposed; tags + gate ship together or neither). Scope bound recorded (Guarino full-vector-all-40 vs Allemang ±R/±I-<10). If operator declines the gate → REJECT-for-now (per-category signatures + ODRs are the record). Status proposed; operator ratifies. |
| 2026-06-15 | [session-043](./session-043-transaction-phase-and-ufocategory-dereferenceability.md) | Full Council (6 voices; `agent-fan-out`, file-based positions + queen-composed — synchronous `Agent` tool can't set `team_name`) | Elisa Kendall | Ian Davis | Transaction lifecycle-phase property + ufoCategory dereferenceability — the two deploy-fix (`4905c6b`) follow-ups (Q1 property-vs-milestones; Q2 reuse-vs-mint scheme; Q3 value-mapping; Q4 dereference inert annotation; Q5 site-model surfacing) | **REVISE/AFFIRM — mint `opda:transactionStatus` + dereference `/pdtf/ufoCategory`.** **(A) Q1 3–1–2:** mint `opda:transactionStatus` on the Transaction Relator (anti-rigid Phase standing on the endurant, ≠ the perdurant milestones — Guizzardi); value = the **existing** `opda:TransactionStatusScheme`; where a milestone log exists the value is `prov:wasDerivedFrom` the terminal milestone (Moreau, drift-auditable), where none exists (chain/lease exemplars) it is the sole carrier of an otherwise-unrepresentable phase. **Q2 4–0–2:** REUSE the scheme, do NOT mint a coarse one (one-primary-scheme IC; coarse tokens collide with `ParticipantStatusScheme`); adopted editorial fix: scheme `skos:definition` "Substance Kind"→"Relator". **Q3 5–0–1:** `completed→Completed`; `active→Offered` (operator-confirmable; `active` is a participantStatus token, not a native transaction phase). **(B) Q4 6–0–0 + Q5 5–0–1:** dereference `/pdtf/ufoCategory` with its DOLCE disclosure (realises ODR-0030 R7b + ODR-0031 §Confirmation — an execution gap, not a new decision; inertness ⊥ dereferenceability — Gandon, the `rdfs:label` analogy); surface `owl:AnnotationProperty` via a distinct `entryKind: 'annotation'` (never folded into the reasoned `'property'` arm), inert badge + disclosure-as-spine, `closeMatch`→gUFO never reaching the reasoner. **DA Davis: WITHDREW Q2/Q4/Q5; HELD Q1 (mint-nothing) + Q3 (drop `active`)** with bounded re-open triggers (a measured milestone-projection bottleneck; WG source recording a standalone transaction phase). Amends ODR-0007 §Rules; confirms ODR-0030 R7b + ODR-0031. Greenfield; status proposed pending WG. |
| 2026-06-17 | [session-047](./session-047-relationship-layer-object-properties.md) | Full Council (7 voices / 7 teammates; `agent-fan-out` + SendMessage cross-talk team `council-047`; `/ruflo-swarm:swarm` init) | Elisa Kendall | Ian Davis | ODR-0032 relationship-layer ratification + ADR-0048 (Q1 un-freeze; Q2 §R1 criterion; Q3 `opda:Name` class-vs-value; Q4 `playedBy`; Q5 OWL-vs-SHACL + rangeless ban; Q6 Address reuse) | **Option A RATIFIED with amendments; full convergence — DA withdrew every attack, no held dissent.** **Q1 6-0-1** (Davis ABSTAIN; §R4 records the bespoke-predicate-vocab pre-commitment as reversible). **Q2 7-0-0 REVISE** — rekey §R1 from source-containment → **endpoint-IC** (both endpoints +I) + three-bucket gate-checked residue register (GATED / VALUE-SLOT / PENDING-upstream-IC) + restored ODR-0022 §G3 per-edge worked-query. **Q3 7-0-0** — **`opda:Name` STRUCK from the object-property inventory** → structured datatype value (−I; no `rdfs:domain`; SHACL `NameShape`); promote to a bounded dependent (no-`owl:Class`) node only on an addressability trigger — **corpus-verified NOT fired** (`NameChangeEvent` names the Person, names are string literals). **Q4 7-0-0** — emit `playedBy` **OPTIONAL/distinct-node** (qua-individual; Davis drift attack withdrawn). **Q5 7-0-0** — gate = **"type-pinned in OWL OR SHACL"** (FAIL on rangeless-AND-shapeless, not rangeless; `rdfs:domain` only where universally true; bearer disjunctions → SHACL `sh:or`; INFER-not-constrain MUST-warning; two-graph separation); **`founds`/`mediates` type-pinned in SHACL `sh:class`** (Guizzardi moved on corpus evidence — `mediates` range-unpinned today — preserving "never reasoned"; the OWL-entailment path a deferred option overriding ODR-0029/0030/0031). **Q6 7-0-0** — reuse single **`opda:Address`**, extend `hasAddress` (drop `rdfs:domain` → SHACL), **class/IC PENDING ODR-0015** (gate must not manufacture an Address class). Convener re-verified the corpus at file:line (`NameChangeEvent`→Person; `founds`/`mediates` "never reasoned"; `mediates` range-unpinned; `playedBy` absent). Amends ODR-0032 §R1/§R2/§R4/§Confirmation + ADR-0048 gate/Phase-1; both `council: session-047`. Greenfield; status proposed pending WG. |
| 2026-06-17 | [session-048](./session-048-owl-documentary-domain-range-r2-set.md) | Full Council (5 voices / 4 teammates; `agent-fan-out` + SendMessage cross-talk team `council-048`, no hive-mind) | Elisa Kendall | Kurt Cagle | OWL-as-documentary-signal: domain/range form + the R2-excluded set (Q1 disjunction form; Q2 Functional/InverseFunctional; Q3 disjointWith) → ADR-0049 | **Q1 (a) repeated-domain dead 0–4** (RDFS §3.2 conjunction); **(b) `owl:unionOf` 3–1 — SUPERSEDED by [session-050](./session-050-adopt-semantic-modelling-owl-coverage.md) Q1** (→ repeated-`rdfs:domain` "any-of", on the hm-coverage adoption). **Q2 AGAINST authoring FP/IFP 4–0** (IFP out entirely — its meaning *is* `owl:sameAs`, banned by ODR-0005/0017; FP → SHACL `sh:maxCount 1`/`dash:uniqueValueForClass`). **Q3 scoped `owl:disjointWith` 3–1** (rigid Kinds only, never anti-rigid Roles, pairwise; DA conditional-withdraw → no held dissent). Pivotal: **Hendler withdrew the "everything-becomes-a-Person" premise as applied to OPDA** (authored-but-never-evaluated is sound by construction — ADR-0035 zero-triple proof). Per-construct "engineering-act=ontological-act" discipline adopted. DA Cagle: Q1 PARTIAL-WITHDRAW+HOLD (omit-now; moot under session-050), Q2 WITHDRAW-into-convergence, Q3 conditional-WITHDRAW (cost-discipline; retired in session-050). → ADR-0049. Greenfield; status proposed. |
| 2026-06-17 | [session-049](./session-049-single-canonical-ufo-typing-axis.md) | Full Council (4 voices / 3 teammates; `agent-fan-out` + SendMessage team `council-048` shared, no hive-mind) | Giancarlo Guizzardi | Ian Davis | Single canonical UFO typing axis — corpus-wide re-typing vs the invariant (Q1 adopt corpus-wide?; Q2 mechanism + scope) → ADR-0050 | **REVISE 4–0 — adopt the single-axis INVARIANT, REJECT corpus-wide rigidity-vector re-typing** (matches hm ODR-0100). Two stale premises rejected: the anti-rigid-subsumption gate "to unlock" already ships green (ADR-0046 checks 8/9); the category axis already exists on 39/40 classes (`SpecialCategoryScheme` exempt — scope to first-order universals). Adopt (atomic): (1) state the invariant (`ufoCategory` canonical; rigidity/identity a *derived view*); (2) axis-consistency meta-shape over the 8 tagged classes; (3) edge-targeted growth-frontier guard. Formal core (Guarino): rigidity must be a *function* — two parallel axes make the SHACL gate's check ill-typed (checker totality). **VERIFIED BUG:** `_ONTOCLEAN_TAGS` (`ufo_categories.py:298-315`) is a hand-maintained 8-row dict whose docstring falsely claims `g(ufoCategory)` derivation — a latent session-047-class defect; fix = automate the projection. DA Davis: WITHDRAW (held edge-participant-scope, non-blocking; re-open if a vector lands on an edgeless class or the rule ships as prose without a gate). → ADR-0050. Greenfield; status proposed. |
| 2026-06-17 | [session-050](./session-050-adopt-semantic-modelling-owl-coverage.md) | Full Council (5 voices / 4 teammates; `agent-fan-out` + SendMessage cross-talk team `council-050`, no hive-mind) | Elisa Kendall | Jim Hendler | Adopt semantic-modelling's OWL coverage as OPDA baseline (hm ODR-0014/0030/0054/0028b) + work out consequences; supersede session-048 Q1 (Q1 repeated-domain; Q2 FP/IFP wholesale-vs-carve-out; Q3 ODR-0030 table + disjointness; Q4 consequences) → ADR-0049 | **5–0 unanimous on all four; no held-as-live dissent.** Adopt hm's coverage **per-construct**, with one principled carve-out. **Q1** repeated-`rdfs:domain` "any-of" (hm ODR-0014; supersedes session-048's `owl:unionOf`), under a CI-gated documentation discipline; `sh:or` carries enforcement. **Q2** CARVE OUT FP/IFP from hm ODR-0054 — IFP out entirely, FP no general layer (`dash:uniqueValueForClass` scoped WITHIN each sortal, never cross-sortal); **binding reason (ii): a published IFP asserts the negation of ODR-0005's 12-0 bounded-context-identity ruling** (re-open only if ODR-0005 overturned); (i) session-035 redundancy corroborating. **Q3** adopt ODR-0030 table re-keyed + excluded-construct meta-shape (**+fail-on-IFP**; keeps `owl:unionOf` excluded — Q1's payoff, corpus stays boolean-constructor-free); `owl:disjointWith` under the **three-part bar: rigid sortals + INCOMPATIBLE (not complementary) ICs + real hazard** → standing scope `Person`/`Organisation` ALONE, Relator deferred, anti-rigid Roles + co-referring Property-family Kinds excluded, pairwise. **VERIFIED:** both gates (`fuseki-load.mjs:277`, `inference_closure_test.py:282`) consume authored pairwise `owl:disjointWith` directly. **Q4** relationship layer regains documentary domain/range; `ci-object-property-coverage` limb (b) inverts; `ontology-model.mjs` keep belt-and-braces; **`founds`/`mediates` authoring GATED on the ODR-0032 §R1 + session-047 Q5 amendment first** (ADR ⊀ ODR cross-corpus). DA Hendler: Q1 HOLD→concurrence, Q2 HOLD-blocking (satisfied), Q3/Q4 WITHDRAW. Supersedes session-048 Q1 only (Q2/Q3 survive). → ADR-0049 (amended). Greenfield; status proposed. |
| 2026-06-30 | [session-051](./session-051-relationship-residue-completion.md) | Full Council (7 voices; `agent-fan-out` + SendMessage cross-talk team `council-051`) | Elisa Kendall | Ian Davis | Relationship-residue completion (orphan-class connectivity) — events, info-objects, verification, lease-term, chain + diagram subClassOf (Q1–Q6) → ODR-0034 + ADR-0055/0056 | **Full convergence, no held-as-live dissent.** **GATED 7–0** the two exemplar-attested-but-TBox-undeclared predicates `opda:partOfTransaction` (Milestone→Transaction) + `opda:concerns` (Transaction→LegalEstate) — declare+gate, not mint. **USE-EXISTING-PROV** (no `opda:` mint) for VerificationActivity `prov:used`/`qualifiedAttribution`, Comparable `prov:wasInformedBy`, NameChange `prov:wasRevisionOf`, UPRNSuccession `prov:wasDerivedFrom`. **RESIDUE-PENDING** `opda:aboutProperty` (info-objects→Property — **Moreau: PROV is silent on aboutness**, Kendall conceded; zero exemplars → bar (b) unmet) + chain `dependsOnTransaction`/`chainMembers` (stale "no exemplar" defer corrected). **VALUE-SLOT 7–0** LeaseTerm (`time:ProperInterval`). **Q6 render `rdfs:subClassOf` FOR 7–0** (muted/optional layer → ADR-0055). New §R5 doctrine: **aboutness ≠ provenance**. DA Davis WITHDREW on Milestone+concerns (exemplar+query bar met), HELD Q2/Q4 with register-satisfied re-open triggers, accepted chain RESIDUE-PENDING. → ODR-0034 (pattern, proposed) + ADR-0055 (diagram) + ADR-0056 (emission). |

**Pending:**

- ~~**A9 amendment session — Gandon-Guizzardi methodology gap**~~ — **CLOSED** by [session-a9](./session-a9-gandon-guizzardi-methodology-gap.md) (2026-05-27, Reduced Council, Queen Kendall, DA Guarino — withdrew). ODR-0001 amended with §What an ODR records (per-kind discipline). The MUST-level IC discipline for `kind: pattern | mapping` is the load-bearing rule downstream sessions inherit.
- ~~**B2 pilot — Session 005** — Property identity crux gate; `consensus-mode: hive-mind/byzantine`~~. **CLOSED** by [session-005](./session-005-property-land-identity-crux.md). 3-class commitment lands (6-2-1); namespace block carries; downstream ODR-0006/0007/0015 unblocked; ODR-0008 deferred. **B2 pilot verdict: EXTEND CAUTIOUSLY** to S015 + S011 Q8.
- ~~**Session 015 — ODR-0015 Address & Geography** (Reduced Council, Phase 2.6 gate)~~. **CLOSED** by [session-015](./session-015-address-and-geography.md). `opda:Address` commits to UFO Substance Kind; 5 of 8 questions withdrawn by DA, 3 conceded, 1 held-as-live (Q3 class-structure with named re-open trigger). Second `kind: pattern` ODR to discharge under A9.
- ~~**B3 pilot — Session 011 Q8**~~. **CLOSED** by [session-011](./session-011-enumeration-vocabularies.md). Third `kind: pattern` ODR to discharge under A9 (at the scheme level). Full Gandon DA withdrawal. Seven-category UFO framework adopted with dual `dct:source`. **B3 pilot third-site EXPAND threshold satisfied** — two-artefact discipline becomes default across remaining sessions.
- ~~**ODR-0001 EXPAND amendment** (Author-only follow-up to S011)~~. **CLOSED 2026-05-27.** ODR-0001 amended in-place: new subsection §Two-artefact discipline (default for Full Council + Reduced Council); §Session document conventions Tally appendix line updated (MUST for Full + Reduced regardless of consensus-mode). Pilot-status framing retired; consensus-mode framework table retains the modes as operational tools selected per session shape.
- ~~**`opda-shacl-af-quality-rules` `pattern`-extraction spawn** (Author-only follow-up to S011)~~. **CLOSED 2026-05-27.** [ODR-0017 — SHACL-AF Non-Blocking Data-Quality Rules Pattern](../ODR-0017-shacl-af-quality-rules-pattern.md) authored as `kind: pattern`. Four citing sites retrofit `implements: [ODR-0003, ODR-0017]` in frontmatter: ODR-0005 §6a (UPRN succession); ODR-0011 §5a (deprecation-chain); ODR-0015 §4a (INSPIRE succession); ODR-0009 §pending (PROV-O Claims when S009 ratifies). Method/plan code UFO category per ODR-0011 §8a + Guizzardi & Wagner 2010.
| 2026-05-27 | [session-006](./session-006-agents-and-roles.md) | Full Council (8 runs: 1 formal-pair + 1 enterprise-pair + 1 governance-pair + 1 bounded-context-extended + 1 shacl-solo + 1 DA + 1 Queen synthesis); two-artefact discipline (DEFAULT per ODR-0001 EXPAND) | Giancarlo Guizzardi | Dean Allemang (DA — 3 withdrew / 3 conceded / **1 held-as-live on Q6**) | Agents & Roles — Phase 3a gate (7 questions: Person/Org IC; RoleMixin vs Role; Proprietorship Relator; Capacity vs Authority; Address reuse; W3C Org Ontology; participantStatus Phase) | **Fourth `kind: pattern` ODR to discharge under A9 — methodology stabilised; 4-of-4 discharge cleanly.** Q1 10-0 FOR Person/Org as Substance Kinds + FIBO multi-identifier IC + plural-regulator-citation + property-level DPV + Cagle SHACL-AF identifier-succession rule (**ODR-0017 fifth citing site**); Q2 10-0 FOR RoleMixin/Role distinction (Allemang DA PRIMARY ATTACK withdrew on conveyancing-engagement-enforcement consumer query — RoleMixin returns Person + Org Sellers in one SHACL pass; sortal-only requires UNION); Q3 10-0 FOR Proprietorship as UFO Relator (Allemang DA withdrew on HMLR open-register API dereferenceability); Q4 10-0 FOR two-predicate Capacity/Authority split as non-negotiable bounded-context seam between Sales and Conveyancing (Evans+Vernon load-bearing) + Cagle three-tier severity match rule (**ODR-0017 sixth citing site candidate**); Q5 10-0 CONCEDE to ODR-0015; **Q6 9-1 FOR `opda:Organisation rdfs:subClassOf org:Organization`** with Allemang DA held-as-live dissent + named 18-month re-open trigger ("downgrade to `dct:source` reference if no consumer exercises Org Ontology machinery beyond class declaration"); Q7 10-0 CONCEDE to ODR-0011 §8a Phase label. Council:session-006 set; `implements: [ODR-0003, ODR-0017]` retrofit; `depends-on:` extends to include ODR-0015. **status:proposed retained** per inherited ODR-0004 namespace block. Downstream: ODR-0007 + ODR-0009 + ODR-0012 + ODR-0013 unblocked; **ODR-0008 STILL deferred** on S005 cardinality (S006 closure does NOT release). |
- ~~**Session 006 — ODR-0006 Agents & Roles** (Phase 3a gate)~~. **CLOSED 2026-05-27.** Fourth `kind: pattern` ODR to discharge under A9. Allemang DA: 3 withdrew / 3 conceded / **1 held-as-live on Q6 Org Ontology depth** with 18-month re-open trigger. ODR-0017 fifth citing site landed (Q1 identifier-succession); sixth citing site candidate flagged (Q4 capacity-authority match). Downstream ODR-0007 + ODR-0009 + ODR-0012 + ODR-0013 unblocked.
- ~~**Session 007 — ODR-0007 Transactions & Lifecycle** (Phase 3b)~~. **CLOSED 2026-05-27** by [session-007](./session-007-transactions-and-lifecycle.md). **Fifth `kind: pattern` ODR to discharge under A9 — 5-of-5 clean.** Davis DA: 2 withdrew (Q1 Relator-URI, Q4 dual-mechanism) / 4 conceded / 1 conceded-with-watch (Q7 OWL-Time scope-creep re-open trigger). Q1-Q7 all 10-0 FOR. Q4 dual-mechanism chain modelling withdrew on three named consumer queries. Q5 lease-term `time:ProperInterval` + **ODR-0017 7th citing site** (LeaseTermSuccessionRule). Q6 Plan-vs-Activity expected/actual + **ODR-0017 8th citing site candidate** (MilestoneVarianceRule). ODR-0007 retrofit `implements: [ODR-0003, ODR-0017]` + depends-on extends to ODR-0006/0011. Downstream: ODR-0009 unblocked.
- ~~**Session 009 — ODR-0009 Claims, Evidence & Provenance** (Phase 4 lead)~~. **CLOSED 2026-05-27** by [session-009](./session-009-claims-evidence-provenance.md). **Sixth `kind: pattern` ODR to discharge under A9 — 6-of-6 clean.** Queen Moreau (PROV-O editor, extended); DA Guarino (PROV-O ≠ assurance — 6 conceded / 2 withdrew on §Rules-text amendments). Q1-Q8 all 10-0 FOR (Q8 defers to ODR-0016). Q3 + Q5 Guarino DA mild attacks withdrew on §Rules-text amendments: Q3 "PROV-O is derivation; assurance is regulated judgement; not derivable from PROV-O traversal"; Q5 `dct:conformsTo` primary + SKOS reification supplement. **Q6 4th citing site of class-level DPV co-annotation pattern (S005 + S015 + S006 + S009); spawn-rule fires** for shared `opda-dpv-class-level-coannotation` pattern ODR. **Q7 ODR-0017 NINTH citing site** (Cagle's `opda:VerificationActivitySuccessionRule` — first citing site explicitly anticipated in ODR-0017 §References). ODR-0009 retrofit `implements: [ODR-0003, ODR-0017]` + depends-on extends to ODR-0006/0011/0015. Downstream: **ODR-0012 UNBLOCKED**.
- ~~**`opda-dpv-class-level-coannotation` pattern-extraction spawn** (Author-only follow-up to S009)~~. **CLOSED 2026-05-27.** [ODR-0018 — DPV Class-Level Co-Annotation Pattern](../ODR-0018-dpv-class-level-coannotation-pattern.md) authored as `kind: pattern`. Pattern: class-level `dpv-pd:hasPersonalDataCategory` baseline on PII-bearing Kinds + variant-conditional refinements via mapping tables consumed by ODR-0012 at generation time. Four citing sites retrofit `implements: [ODR-0003, ODR-0017, ODR-0018]`: ODR-0005 §3c (RegisteredTitle published-PII); ODR-0015 §7a (Address baseline + 3 variants); ODR-0006 §Q1+Q4 (Person identifier predicates property + class level); ODR-0009 §Q6 (Evidence subclasses baseline). UFO Quality category per ODR-0011 §8a + Guizzardi 2005 Ch. 4 + Masolo D18 §4.3 + ODR-0018 (Council-authored DPV-Quality binding). Three-graph separation: DPV co-annotations in `opda-annotations.ttl`; CI tests verify absence from shapes graph + class graph. ODR-0012 (when ratified) will be canonical authoring authority.
- ~~**Session 012 — ODR-0012 Data-Governance Layer** (Phase 4 follow)~~. **CLOSED 2026-05-27** by [session-012](./session-012-data-governance-layer.md). `kind: architecture` ODR (relaxed A9 regime; follows ODR-0001/0003/0004 architecture pattern). Queen Pandit (DPV CG chair; authoring authority per Scope-Check 1 Q5 + ODR-0018 §7); DA Kendall (reference-not-import — 5 conceded / 2 withdrew on §Rules-text amendments). Q1-Q7 all 10-0. Q2 **Pandit's S001 dissent VINDICATED** via reference-not-import mechanism per ODR-0018 (Kendall DA condition met — `dct:source` to DPV CG with version pin; mapping tables OPDA-authored; DPV TBox external). Q3 Article-10 special-category depth with Cagle `sh:Violation` SHACL + Baker `opda:SpecialCategoryScheme` + named stewards. Q4 ODRL deferral with **three named activation triggers** (Phase-2 VC consent receipts; OPDA TF policy instances; consumer regulatory requirement). Q5 Cagle PIIWithoutDPVCoAnnotation SHACL-AF rule — **ODR-0017 TENTH citing site**. ODR-0012 retrofit `implements: [ODR-0003, ODR-0017, ODR-0018]` + depends-on extends to ODR-0006/0009/0011/0015/0018. Downstream: ODR-0010 + ODR-0013 inherit DPV profile-filtering + special-category severity.
- ~~**Session 010 — ODR-0010 Overlay Profile Mechanism** (Phase 5)~~. **CLOSED 2026-05-27** by [session-010](./session-010-overlay-profile-mechanism.md). Reduced Council (5 runs: Cagle-solo + pragmatic-pair + enterprise-pair + Guarino-DA + Queen Knublauch synthesis). Two-artefact discipline. Queen Knublauch (extended SHACL co-author); DA Guarino (S001 Q5 "no fixed model theory" carry — 7 conceded / 1 withdrew on Q1 UFO Substance Kind §Rules amendment). `kind: architecture` ODR (relaxed A9 except for `opda:ValidationContext` Kind). Q1-Q8 all 10-0. **Q1 5-property `opda:ValidationContext` reification + UFO Substance Kind commitment** discharging S001 Q5 concession fully. **Q6 no-identity-override SHACL meta-shape at `sh:Violation` — ODR-0017 11th citing site triggers §2a amendment** (admit `sh:Violation` severity for meta-shape-over-shape-graph; flagged for follow-up Author-only). Q7 BASPI5 round-trip as MVP gate (methodology coherence pressure-test). Q8 three-rule interface contract with ODR-0013 (Cagle Scope-Check 1 Q6). ODR-0010 retrofit `implements: [ODR-0003, ODR-0017]` + depends-on extends. Downstream: ODR-0013 inherits three-rule interface contract.
- ~~**Session 013 — ODR-0013 SHACL Validation & Severity** (Phase 6 CLOSING)~~. **CLOSED 2026-05-27** by [session-013](./session-013-shacl-validation-and-severity.md). Full Council (5 runs: formal-pair + pragmatic-pair + Moreau-extended + Cagle-DA + Queen Knublauch synthesis). Two-artefact discipline. Queen Knublauch; DA Cagle (S001 Q5 aiHint exile carry — 5 conceded / 1 withdrew on Q4 PRIMARY VIGILANCE). **`kind: architecture` ODR — fifth and final architecture discharge** (after ODR-0001/0003/0004/0012/0010). Q1-Q7 all 10-0. **Q1 four-tier severity framework with FIVE `sh:Violation` categories** (identity-key missing / IC breach / no-identity-override per S010 Q6 / special-category PII without lawful-basis per S012 Q3 / meta-shape-over-shape-graph drift per ODR-0017 §2a NEW amendment). **ODR-0017 §2a AMENDMENT LANDED INLINE in ODR-0013 §Rules** (closing the S010 Q6 trigger; narrow scope — data-graph `sh:Violation`-NEVER floor preserved). Q4 Cagle DA PRIMARY VIGILANCE WITHDREW on aiHint exile cross-cite (ODR-0004 §3a CI tests + AI-RDF consumer use case named verbatim). Q5 two-layer reporting (SHACL-native + flattened conveyancer UI). Q7 three-rule interface contract cross-cite mechanism operationalising Cagle Scope-Check 1 Q6. ODR-0013 retrofit `implements: [ODR-0003, ODR-0017]` + depends-on extends to all 11 prior ODRs. **status:proposed retained** per namespace block. **THE PLAN IS COMPLETE.** Outstanding: S008 deferred-on-cardinality; S016 deferred-until-trigger; ODR-0010 §References cross-cite Author-only follow-up; Phase-3.5 audit post-MVP. **BASPI5 round-trip is the MVP gate.**
- ~~**Session 007 — ODR-0007 Transactions & Lifecycle**~~. **CLOSED 2026-05-27** (see Track Record above).
- ~~**WG namespace ratification (Session 003b)**~~ — **CLOSED 2026-05-27** by [session-003b](./session-003b-namespace-wg-decision.md). WG selected `https://w3id.org/opda/#` per Knublauch S004 DA primary demand. 13-ODR status sweep executed; exemplar bulk-update applied; [ADR-0006](../../../adr/ADR-0006-w3id-opda-ontology-namespace.md) authored as engineering realisation. **Phase 1 formally closed.** Outstanding engineering: w3id.org redirect PR (asynchronous; tracked in ADR-0006 confirmation gate).
- ~~**WG housekeeping decisions (Session 003c)**~~ — **CLOSED 2026-05-27** by [session-003c](./session-003c-wg-housekeeping-decisions.md). Three small WG decisions: (i) instance-URI namespace `opda-x:` → `https://openpropdata.org.uk/data/exemplar/<name>/` (Author-only; institutional, decoupled from w3id.org; mechanical follow-up sweep across 15 exemplars pending); (ii) hash-vs-slash reopening trigger (1,000-term-per-file volume OR named-consumer per-term-conneg request); (iii) OPDA TF authoritative scope ratification (operationally honoured per ODR-0009 Q5 + ODR-0012 Q2; now formally recorded).
- ~~**Phase-3.5 audit (cross-vocabulary mapping discipline)**~~ — **CLOSED 2026-05-27** by [session-phase-3.5-audit](./session-phase-3.5-audit-cross-vocabulary-mapping.md). Author-only. Three disciplines recorded: (i) SSSOM/SEMAPV defer-until-trigger; (ii) inline `skos:exactMatch` case-by-case admission per consuming module; (iii) OBO RO stays deferred with Kendall+Guizzardi held-as-live preserved.
- ~~**Session 008 — ODR-0008 Property Descriptive Attributes**~~ — **CLOSED 2026-05-27** by [session-008](./session-008-property-descriptive-attributes.md). Full Council (5 teammates + DA + Queen Allemang; two-artefact discipline). All 7 questions 10-0. Cagle DA: 6 of 7 WITHDRAWN/CONCEDED + 1 HELD-AS-LIVE (Q3 section-level) + 1 PRIMARY VIGILANCE (Q7 overlay-handoff boundary). Three other held-as-live dissents preserved (Kendall Q2 four-way alternative; Davis Q4 Building/Room conditional; Kendall Q5 SKOS expansion; Kendall Q6 utility parent). **Seventh `kind: pattern` ODR to discharge under A9 — methodology 7-of-7 clean.** ODR-0008 retrofit `implements: [ODR-0003, ODR-0007, ODR-0011, ODR-0017, ODR-0018]` + depends-on extends to full upstream TBox. **Status: proposed → accepted; council:session-008 set.** **The original 14-session plan is COMPLETE at the Council-ratification level.**
- **Session 016 — ODR-0016 W3C VC/DID Compatibility** — **STAYS DEFERRED** until any of the three named triggers fires (S009 Q8 VC-side decisions; S012 Phase-2 consent receipts; named wallet/DID consumer). Running S016 now would violate the plan's own deferral discipline (Scope-Check 1 Q7c 8-1 verdict).
- ~~**`pattern`-extraction follow-up:** Spawn shared `opda-shacl-af-quality-rules` `pattern` ODR (fourth citing site reached at S011 §5a).~~ **CLOSED** — [ODR-0017](../ODR-0017-shacl-af-quality-rules-pattern.md) is that pattern ODR (authored 2026-05-27); the follow-up is done.
- ~~**ODR-0001 follow-up amendment:** Author-only session recording EXPAND verdict on two-artefact discipline; update §Format tiers / §Substrate operations.~~ **CLOSED 2026-05-27** — ODR-0001 amended in-place (§Two-artefact discipline added); duplicate of the struck S011 EXPAND item above.
- ~~**Instance-URI namespace mechanical sweep:** Bulk-update 15 exemplar `.ttl` files to flip `opda-x:` from `opda.uk/data/exemplar/` to `openpropdata.org.uk/data/exemplar/`.~~ **DONE** (verified 2026-05-31) — all 15 exemplars already declare `@prefix opda-x: <https://openpropdata.org.uk/data/exemplar/…>`; no `opda.uk/data/exemplar/` remains.
- **w3id.org redirect PR:** Submit `/opda/` redirect to perma-id/w3id.org per ADR-0006 confirmation gate. Asynchronous community-paced merge. Draft at `docs/ontology/deployment/w3id-pr-draft.md`.
- **BASPI5 round-trip MVP demonstration:** Implementation programme (ADR-0007 generator → foundation.ttl + module .ttl + SHACL shapes → form regeneration). Multi-day engineering; closes the programme per ODR-0003 retirement criterion.

## §Project-specific when-to-use additions

OPDA extends the general when-to-use list with these domain-specific categories:

- **PDTF schema boundaries** — bounded-context boundaries between PDTF v3's base transaction schema and its 10+ overlay schemas (BASPI, TA6/7/10, NTS/NTS2, CON29R/DW, LLC1, LPE1, FME1, OC1).
- **Property-transaction overlays as SHACL profiles** — the overlay-profile mechanism (ODR-0010) and its interaction with severity (ODR-0013) are sufficiently load-bearing that questions about their boundary warrant Council.
- **eIDAS / OIDC4IDA / W3C VC interop** — the verifiedClaims envelope sits at the seam between PDTF and the W3C VC ecosystem; alignment questions warrant Council (deferred to ODR-0016 unless triggered earlier).
- **UK-Gov data-sharing alignment** — when OPDA records make commitments that affect interoperability with `gov.uk` OneLogin, HMLR RDF, INSPIRE, or analogous public-sector datasets, Council session warranted.
- **Property identity and registration** — UPRN, INSPIRE Identifier, title-register identity, and the Endurant commitments that ground them (the identity-crux family — ODR-0005 + ODR-0015) are domain-load-bearing enough to warrant Council whenever they're touched.

## §Council directory path

OPDA's Council artefacts live at:

```
docs/ontology/odr/council/
```

with the conventions:

- **Per-record sessions**: `session-NNN-<slug>.md` (e.g. `session-001-pdtf-schema-to-ontology.md`)
- **Meta-Councils**: `scope-check-N-<slug>.md` (e.g. `scope-check-1-programme.md`, `scope-check-2-hive-vs-swarm.md`)
- **Per-teammate working files**: `working/session-N/<teammate>.md` or `working/scope-check-N-<slug>/<teammate>.md`
- **This adoption record**: `adoption.md` at the council-directory root.

## Adoption-record maintenance

This record is updated when:

- A new Council session lands (track-record table grows).
- A meta-Council amends OPDA's project-specific slots (e.g. promotes an extended-panel candidate to pre-elected).
- The Real-world Governance Handoff changes (e.g. OPDA AGM restructures into a different ratification chain).

Updates land as in-place edits with git history as the audit trail. No parallel amendment-record file (per DCMI hygiene + the methodology's self-amendment guidance).

## References

- Methodology: [ODR-0001 — Linked Data Council: Review Methodology](../ODR-0001-linked-data-council-methodology.md).
- Programme anchor: [ODR-0003 — PDTF → Ontology: Programme & Work Breakdown](../ODR-0003-pdtf-ontology-programme.md).
- Plan: [Council follow-up sessions](../../../plan/council-followup-sessions.md) — operational plan for the 13 active ODR ratification sessions plus the deferred ODR-0016.
- Sister project precedent: H&M semantic-modelling programme (the source of the Council methodology; see ONT-0021 in that repository).
